# SEO-план матрицы судьбы (мастер)

Цель — запуск сайта `arcana-sense.ru` на **органике**: энциклопедия собирает информационные хвосты
толкования, самый крупный рычаг — страницы арканов. Это верхний документ: держит стратегию,
техническую готовность, авторитет, измерение и порог запуска, а детали делегирует под-планам.

## Карта документов (что где)

| Слой SEO | Документ | Внутри |
|---|---|---|
| **Контент / семантика** | [`article-plans/article-total-plan.md`](article-plans/article-total-plan.md) | категории, сколько статей, суммы спроса |
| **Требования к статье** | [`article-requirements.md`](article-requirements.md) | смысл/качество, on-page, формат сдачи |
| **Код / приложение** | [`site-plan.md`](site-plan.md) | роуты, шаблоны, схема данных, тех-SEO, пайплайн |
| **Стратегия / готовность** (этот файл) | `plan.md` | слои, авторитет, измерение, порог запуска |

Здесь спрос и категории **не дублируются** — они в `article-total-plan.md`. Дев-задачи — в `site-plan.md`.

## Слои полного SEO и их покрытие

Контент-план — это один слой (крупнейший, но не всё SEO). Полная картина:

Легенда статуса: ✅ готово · 🟢 готово в коде/контенте, на бою после деплоя · 🟡 частично · ⬜ не начато · ⚠️ гейт.
Аудит на 28.08 (всё сделанное — в репозитории; прод ещё старый, единый оставшийся блокер — **деплой**).

| # | Слой | Где закрыт | Статус |
|---:|---|---|---|
| 1 | Контент / семантика | `article-plans/` | ✅ весь корпус написан (арканы 22, хабы 5, на год 24, позиции 20, хвосты 76, сочетания 231) |
| 2 | Техническое (индексация, sitemap, schema, noindex дублей, CWV) | `site-plan.md` | 🟢 в коде (noindex матриц, sitemap без них, Article/FAQPage/BreadcrumbList, canonical, robots) |
| 3 | On-page (title/H1/meta/структура) | `article-requirements.md` + `site-plan.md` | 🟢 в коде (H1 «N в матрице», крошки везде, meta/OG) |
| 4 | Авторитет / off-page / E-E-A-T | §4 ниже | 🟡 «о методе» + «автор» ✅ (написаны, роуты, футер); off-page/упоминания ⬜ |
| 5 | Измерение / мониторинг | §5 ниже | 🟡 Вебмастер + GSC ✅; цели Метрики ⬜ |
| — | Порог запуска | §6 ниже | ⚠️ гейт: деплой + цели Метрики |

---

## 0. Первым делом — быстрые победы (без единой статьи)

Что можно сделать **сразу, дёшево и до написания контента**. Отсортировано по отдаче.

**Код (одна выкладка, часы):** — все 5 сделаны в коде (🟢), на бою после деплоя.
1. ✅ **Арканы: title/H1 под «N в матрице судьбы»** — H1 `{n} в матрице судьбы: {title}` (§3, §7).
2. ✅ **Калькулятор (CalcPromo) на все инфо-шаблоны** — стоит на арканах, чакрах, сочетаниях, позициях (§3).
3. ✅ **5544 матрицы → `noindex, follow`** + вон из `sitemap.ts` — сделано (§3, §6).
4. ✅ **Убрать медицинские формулировки** — гигиена-фильтр `content.ts` режет BANNED-лексику целым полем (§4).
5. ✅ **`BreadcrumbList` во все шаблоны**; `FAQPage` где есть Q&A — оба сделаны (§3).

**Ops (после выкладки на домен, 0 ₽, минуты):**
6. Подтвердить домен в **Яндекс.Вебмастере + Google Search Console**, отправить sitemap. (§5, §6)
   - ✅ Яндекс.Вебмастер: домен подтверждён (файл `/yandex_00cc019ab4679e8d.html`, 27.08).
   - ✅ Google Search Console: домен подтверждён (DNS TXT `google-site-verification=njbmyy2…`, 28.08).
   - ✅ sitemap найдут сами через `robots.txt` (`Sitemap:` прописан). Ручная отправка в интерфейсе — по желанию: ускоряет старт и даёт отчёт по карте.
7. ⬜ **Метрика:** настоящий ID + цели (`calc`, `buy_click`, `pay_open`, `lead`, `purchase`) —
   код шлёт `reachGoal`, но цели в счётчике **не заведены** → расчётов и покупок не видно. **Единственный незакрытый Ops-пункт.** (§5, §6)
8. ✅ `robots.txt` закрывает приватное (`robots.ts`: /account,/admin,/report,/matrices,/pay,/login,/register,/api/); `sitemap.ts` без матриц. Проверка 2xx на бою — после деплоя. (§6)

**Дёшево, чуть контента (1–2 страницы):**
9. ✅ Страница **«о методе» + автор** (E-E-A-T) — написаны (`hubs/o-metode.json`, `hubs/avtor.json`), роуты `/o-metode` `/avtor`, ссылки в футере. (§4)

Самые выгодные первые шаги — **№1 и №2**: ноль контента, правка шаблонов, работают на крупнейший
кластер спроса и на конверсию всего инфо-трафика.

---

## 1. Гипотеза и главный вывод

- Ставка на органику по инфо-хвостам верна цифрами: толкование (числа, арканы, позиции, карма)
  ≈ 0,97 млн показов/мес против ~0,45 млн у самого калькулятора; 71% фраз — 50–300 показов, но их
  тысячи. Энциклопедия — правильный сборщик этого хвоста.
- **Приоритет №1 — не хвосты, а 22 страницы арканов** (505k спроса, уже написаны): докрутить
  title/H1 и обогатить = максимум отдачи за минимум работы. Разбор — в `article-total-plan.md`.
- Реальный потолок спроса ≈ корень «матрица судьбы» ~1 млн **показов** (не визитов); суммы кластеров
  пересекаются по запросам и годятся для сравнения, а не как трафик.

## 2. Что уже сделано (аудит) — сильная база

`sitemap.xml` единый, `robots.txt`, `canonical` на каждой странице, OG/Twitter, JSON-LD `Article`
на арканах, перелинковка в обе стороны, локальные шрифты (CWV, 152-ФЗ), 404 → `noindex`, `lang=ru`.
Технический каркас в основном закрыт; доработки и пробелы — в `site-plan.md`.

## 3. Техническая готовность (сводка; детали — site-plan.md)

Что должно быть верно к запуску (всё ниже — сделано в коде, ждёт деплоя):
- ✅ 5544 матрицы день-месяц-год → `noindex, follow` + вон из sitemap (массив почти-дублей).
- ✅ `BreadcrumbList` во все шаблоны, `FAQPage` где есть Q&A.
- ✅ Арканы: title/H1 под «N в матрице судьбы».
- ✅ Контент-формат: статьи пишутся сразу как JSON по схеме категории (`content/*.json`), не в markdown —
  моста-конвертера не нужно (см. `article-requirements.md` Часть D, `site-plan.md` §4).

## 4. Авторитет и доверие (E-E-A-T / off-page) — слой, которого не было

Тематика — YMYL-смежная (судьба, отношения, «карма»), поэтому доверие к источнику влияет на ранг.

- ✅ **Страницы доверия:** «о методе» + «автор» написаны (роуты `/o-metode` `/avtor`, футер); реквизиты,
  контакты, дисклеймер (`lib/site.ts`) — есть. Автор = проект Arcana Sense (ИП), без вымышленных регалий.
- ✅ **YMYL-осторожность:** медицинских формулировок и обещаний событий нет (гигиена-фильтр режет BANNED).
  Чакры как эзотерика — ок; «здоровье/вес/органы» под нож.
- ⬜ **Начальные ссылки/бренд:** 2–3 качественных тематических упоминания, профили, узнаваемость
  «Arcana Sense». Не начато (после запуска). Без закупки ссылочного — оно вредит в этой нише.

## 5. Измерение и мониторинг

- ✅ **Яндекс.Вебмастер + Google Search Console:** домен подтверждён в обоих (28.08); sitemap найдут
  через `robots.txt`. После запуска — следить за индексацией, долей «малополезных», позициями.
- ⬜ **Яндекс.Метрика:** ID стоит, но цели (`calc`, `buy_click`, `pay_open`, `lead`, `purchase`) не
  заведены; `notBounce` на 15 с. **Открыто — заводится в интерфейсе после деплоя.**
- **Что мониторим после запуска:** % страниц в индексе, позиции по категориям (арканы, хвосты, на год),
  органик-трафик и конверсия инфо-трафика в расчёт/лид.

## 6. Порог запуска (P0) — без этого органику не лить

- ✅ Домен подтверждён в обоих вебмастерах (28.08); sitemap найдут через `robots.txt`. Проверка 2xx — на бою после деплоя.
- ✅ Приватные адреса закрыты (`robots.ts`: `/report`, `/account`, `/pay`, `/matrices`, /admin, /login, /register, /api/).
- ⬜ **Метрика с целями** — цели не заведены (SEO без измерения бессмысленно). Открыто.
- ✅ 5544 матрицы под `noindex`, sitemap пересобран (без матриц).
- ✅ Медицинские формулировки убраны (гигиена-фильтр).
- Юридическая/продуктовая готовность (реквизиты, оферта, касса) — в `docs/site-launch-plan.md`, не здесь.

**Общий блокер над всем P0 — деплой:** контент и код готовы в репозитории, но прод ещё старый
(sitemap 5848 с матрицами, `/o-metode` и хвосты = 404). До выкладки ни один пункт не работает на бою.
Остаётся ровно два незакрытых дела: **① деплой, ② цели Метрики** (заводятся в интерфейсе после выкладки).

## 7. Дорожная карта (порядок фронтов)

1. **Порог запуска (§6)** — тех-готовность и измерение.
2. **Тех-SEO** — `site-plan.md` §1 (noindex матриц, schema, title, калькулятор на инфо-страницы, sitemap).
3. **Обогащение арканов** — `article-plans/01-arcana.md` (топ-рычаг, 505k).
4. **Концепт-хабы → на год → хвосты** — `article-plans/`; схему контента задаём при постройке шаблона
   (`site-plan.md` §4), после развилок: URL-схема и формула хвоста.
5. **Авторитет и мониторинг (§4–5)** — параллельно и после запуска.
