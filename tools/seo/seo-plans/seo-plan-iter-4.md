# SEO-итерация 4: дефекты, найденные на ревью итерации 3

Итерация 3 (`seo-plan-iter-3.md`) помечена как выполненная: 205 из 205 пунктов с `✅`. Ревью по коду,
контенту и живому стенду показало, что каркас построен, но **полностью закрыты 2 раздела из 20**;
остальные 18 закрыты частично. Здесь — только расхождения и то, что надо починить.

Источник фактов: прогон на локальном стенде (`commit 084b2ff+`, mock-оплата), перебор
`web/content/matrices.json` и `content/data/in-positions.json`, прямые замеры движка через vitest.
Числа с пометкой «замерено» получены прогоном, а не оценкой.

---

## Итог: 77 из 77 пунктов закрыто

Последний пункт закрыт решением владельца, а не работой: постраничная печать PDF **отклонена**
(§1.7). Файл читают с экрана, а разбить длинный лист на страницы можно только ценой разорванных
блоков или пустот — одним листом он и сделан поэтому.

**Что дал этот заход, кроме галочек.**

| Что | Числа |
|---|---|
| Грамматика вставки кубиков ролей | «в позиции итог М человек **Дар превращается в…**» — **11 458 → 0** на всех 26 284 разборах |
| Оборонительный регистр статей | отрицаний **40 % → 10 %** предложений, глав с отрицанием **151 → 86** из 164 |
| FAQ под поиск | дословных фраз из `seo.queries` в вопросах **2 → 70** |
| Объём статьи | **450 → 744** слова, глава **45–69 → 70–123** слова |
| Каркас в корпусе | «В сильном прохождении», «риск —», «К следующему этапу» — **0** вхождений вместо 19–22 из 22 |
| Тексты, дошедшие до кубиков | `years` и `body_resource` — **22 из 22** вместо 4 и 0 |
| Пересечение разборов одной матрицы | `resources`~`money40` **154 → 69** общих 8-грамм, `realisation`~`purpose` **214 → 157** |
| 404 маршрутов энциклопедии | оболочка `__next_error__` → «Страница не найдена» с телом на всех проверенных адресах |
| Написанного текста добавлено | 22 текста повтора аркана, 22 текста `money40`, 22 текста `parents_children`, 8-я глава визитки |

**Пять пунктов плана отменены проверкой, а не выполнены.** Ревью ошиблось, и это стоило бы правок:
`money.points` ведёт на верную точку; суффикс `title` работает по описанному правилу; ключевые слова
`comfort` и `chakras` живут в `seo.title`, а сравнили не то поле; 154 «мёртвых» текста публикуются на
22 страницах арканов; 19 «потерянных» формулировок убраны итерацией 3 осознанно и защищены сторожем.

**Цикл живого поиска (§7) добавил к плану 13 своих дефектов** — из них четыре про деньги и доступ,
и один был регрессией правки §1.1. Все закрыты, у денежных и вёрстки — красный тест до правки.

**Полный gate прогнан целиком: `ЛОКАЛЬНЫЙ PREFLIGHT ЗЕЛЁНЫЙ`.**

| Шаг | Результат |
|---|---|
| контракт метода, engine и артефакты | 63 из 63 |
| воспроизводимость таксономии, семантики и URL registry | сошлось |
| схема исходников статей и ordered-хвостов | сошлась |
| целостность редакционного корпуса | **6 065 проверок, 0 ошибок** |
| self-test валидатора | ловит все подставленные случаи |
| контрактные тесты API и платежей | 165 из 165 |
| unit, golden и полный TypeScript parity | **364 из 364** |
| typecheck и production-сборка Next | чисто, 6 357 страниц |
| браузерные сценарии (мок-оплата, callback, refund, PDF) | **279 из 279** (8 банковских отобраны по маркеру) |
| приёмка собранного фронта и manifest | собран, 356 URL в sitemap |

**Одно исключение, и оно не про эту работу.** `npm audit` красный: три высоких уведомления
`postcss` и `sharp`, которые тянет `next`, лечатся только мажорным обновлением до `next@16.3.4`
(breaking change). `package.json` и `package-lock.json` в итерации не менялись — дефект
предсуществующий, решение об обновлении за владельцем. Для верификации остальных шагов gate
перезапущен с `SKIP_NPM_AUDIT=1`; это не решение о релизе.

---

## 0. Как воспроизвести всё, что здесь написано

Раздел служебный: без него числа ниже нельзя ни перепроверить, ни использовать как критерий
готовности правки.

### Пути

**Все пути в этом файле — относительно `project/destiny-matrix/`**, кроме явно начинающихся с
`tools/`: те относительно корня репозитория `/home/sergey/Personal/research-tools`.

### Состояние репозитория на момент ревью

Вся работа итерации 3 лежит **в индексе и не закоммичена**: `HEAD` = `084b2ff`, ветка `main`,
79 файлов со статусом `M `/`A `, рабочее дерево совпадает с индексом, untracked и stash пусты.
Любой `git checkout` / `reset` / `stash` уничтожит итерацию 3 целиком — сначала коммит.

### Ссылки на параграфы

`§1`…`§8` без уточнения — разделы **этого** файла. Ссылки на другие документы всегда подписаны:
«§20.2 итерации 3» → `seo-plan-iter-3.md`, «правило 6» и «правило 7» → «Сквозные правила применения
требований» в начале того же файла, «§3 доки» в разделе 7 → `tools/benchmark-testing/testing-cycle.md`.

### Буквы ролей

Документ везде называет роли буквами — это обозначения из спецификации метода, не переменные кода.
Соответствие позициям матрицы (`web/lib/sectionReadingShared.ts`, `SECTION_ROLES`):

| Буква | Позиция в `Matrix` | Смысл | В каких разделах |
|---|---|---|---|
| A | `day` | визитка, первое впечатление | character |
| B | `month` | врождённый дар | character, profession, soul_tasks |
| C | `year` | материальная задача, бытовая опора | character, body_resource |
| D | `mission` | корневая задача, возвращающийся сюжет | realisation, soul_tasks, body_resource, loops |
| E | `center` | внутренний центр, базовое состояние | comfort, rest, loops |
| M | `comfort_south` | автоматическая реакция | comfort, relations |
| K | `comfort_north` | талант, возвращающий управление | comfort, profession, relations |
| L | `comfort_east` | вход ресурса и денег | resources, money, money40 |
| J | `comfort_west` | автоматическая защита | karma40 |
| F / G | `father_line` / `mother_line` | принципы мужской и женской ветви | family_gifts, parents_children |
| H | `descendants` | что передаётся дальше | parents_children |
| I | `inheritance` | родовое наследство | karma40, ancestry |
| P | `talent[1]` | форма профессиональной реализации | profession |
| R2 | `money[1]`, вариант `resource_direction` | направление ресурса | resources, money, money40 |
| R (деньги) | `money[2]`, вариант `money_choice` | личный денежный выбор | money |
| земля | `ground[2]`, вариант `ground_total` | устойчивость денежного результата | money |
| R1 | `love[1]`, вариант `relation_knot` | главный узел партнёрства | relations |
| R (отношения) | `love[2]`, вариант `relation_form` | форма союза | relations |
| итог неба | `sky[2]`, вариант `sky_total` | общий урок | soul_tasks |
| итог М / итог Ж | `social_male[2]` / `social_female[2]`, варианты `family_male_gift` / `family_female_gift` | дары ветвей рода | family_gifts |
| задача М / задача Ж | те же числа, варианты `ancestry_male_task` / `ancestry_female_task` | родовые задачи ветвей | ancestry |
| личное / социальное / духовное / планетарное | `purpose_personal` / `purpose_social` / `harmony` / `planetary` | четыре уровня предназначения | purpose, realisation, loops, ancestry |
| итог опоры | `chakras[6].emotions`, вариант `body_total` | режим восстановления | body_resource |
| радость | `chakras[5].emotions`, вариант `rest_result` | результат отдыха | rest |
| десятилетие | `age_scale[i].arcanum`, вариант `decade` | тема этапа 0–10…70–80 | years |

Осторожно: **`R` означает разное** в деньгах (`money[2]`) и в отношениях (`love[2]`); а `итог М` и
`задача М` берут **одно и то же число** `social_male[2]`, но обязаны давать разный текст (см. §2.1).

Слаг персональной статьи = значения ролей раздела через `-` в порядке `SECTION_ROLES`
(`sectionReadingSlug`); у `chakras` это 24 числа (7 строк × 3 колонки + 3 итога), у `years` к слагу
добавляется `?birth=YYYY-MM-DD`.

### Стенд

```bash
cd project/destiny-matrix && ./compose/scripts/run.sh      # сборка образов + подъём
curl -s http://127.0.0.1:3000/api/health                    # {"ok":true,"db":true}
docker inspect arcana-api-1 --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -i payment
```

Стенд отдаёт **production-сборку из docker-образа**, а не dev-сервер: после правки исходников
`run.sh` надо запускать заново, иначе браузер показывает прошлую сборку. Признак рассинхронизации —
`mtime` файлов в `web/` новее, чем `docker inspect arcana/web:dev --format '{{.Created}}'`.
Оплата должна быть `PAYMENT_PROVIDER=mock`; при другом провайдере проверки не начинают.

### Наборы тестов — гонять по отдельности

```bash
PY=/home/sergey/miniconda3/envs/research3.12/bin/python
$PY -m pytest engine content -q          # контракт метода и корпус
$PY -m content.validate                  # целостность редакционного корпуса
npm --prefix web test -- --run           # юниты, golden, TypeScript parity
npm --prefix web run typecheck
$PY -m pytest e2e -q                     # браузерные; требуют поднятого стенда
```

Полный локальный gate — `compose/scripts/run-tests.sh`. Он же единственное место, откуда запускается
`content/validate.py`: **в `pytest` этот валидатор не входит**, поэтому «зелёный pytest» не означает
проверенный контент.

### Как замерялись числа этого файла

Замеры сделаны одноразовым vitest-конфигом **вне репозитория** (чтобы не портить `git status`).
Рецепт целиком:

```bash
SCRATCH=/tmp/seo-audit && mkdir -p $SCRATCH
ln -sfn "$PWD/project/destiny-matrix/web/node_modules" $SCRATCH/node_modules

cat > $SCRATCH/vitest.config.mts <<'EOF'
import { defineConfig } from "vitest/config";
const WEB = "/home/sergey/Personal/research-tools/project/destiny-matrix/web";
export default defineConfig({
  resolve: { alias: { "@": WEB } },
  test: { include: ["/tmp/seo-audit/*.test.ts"], root: WEB },
});
EOF
# тест кладём в $SCRATCH/probe.test.ts, импортируем через "@/lib/..."
cd project/destiny-matrix/web && npx vitest run --config $SCRATCH/vitest.config.mts
```

Две тонкости, на которых легко споткнуться: `node_modules` должен быть виден рядом с конфигом (иначе
`vitest` не резолвится), и запускать надо **из каталога `web/`** — загрузчик контента
(`web/lib/content.ts`) читает `content/*.json` относительно `process.cwd()`.

Через этот рецепт получены §1.1 (двойная точка), §1.2 (пропажа дисклеймеров), §1.3 (выброшенные
предложения), §2.1 (совпадение предметной части парных ролей). Перебор `web/content/matrices.json`
обычным Python дал 18 чисел достижимых результатов и §3.5 (доля отрицаний, слоты FAQ).

### Чему в этом файле можно верить как факту, а чему — как отчёту

Ревью делали параллельно несколько исполнителей. Ниже — граница доверия, чтобы никто не принял
непроверенное число за установленное.

**Перепроверено прогоном лично, можно опираться:** все 18 чисел достижимых результатов и формы
повторов · §1.1 (15 194 страницы с `..`) · §1.2 (2 190 разборов без дисклеймера, поразделно) ·
§1.3 (588 из 1 820 предложений; отсутствие нужной фразы на живой странице `years`) · §1.4 (пример
`16–11–3`: движок даёт `11–22–8`, адрес отдаёт 404) · §1.5 (57 и 17 px переполнения) · §1.6 (красный
тест; недостижимость контекстов для пары {3,4}) · §2.1 (общий смысловой хвост парных ролей) ·
§2.3 (154 текста не рендерятся: проверено по 7 корпусам × 22 аркана на живых страницах разделов) ·
§2.4 (11 разделов без примера; 72 из 86 у `D↔социальное`) · §3.5 (227 из 600 предложений с
отрицанием, слоты FAQ, 2 из 104 запросов) · §4.1 (сломанный регексп канцелярита) · §5 (совпадение
тел `repeatedSummary`; 78 общих строк-слагов) · §6 (состояние git и результаты всех наборов тестов) ·
2 743 результата `family_gifts`, где итог М = итог Ж.

**Взято из отчёта исполнителя и мной не перепроверялось** — перед тем как класть в основу решения,
замерить заново:

| Утверждение | Где |
|---|---|
| PDF отчёта — одна страница 14 393 pt при пределе 14 400 | §1.7 |
| 1 720 из 4 463 разборов `family_gifts` с грамматическим браком «…человек Дар превращается в…» | §2.2 |
| дословный повтор клаузы: `character` 724, `comfort` 26, `realisation` 18, `karma40` 20 | §2.2 |
| 356 адресов в боевом sitemap | §0, §4.2 |
| 151 / 191 / 107 общих 8-грамм между персональными статьями одной матрицы | §5 |
| медицинский гард — 19 префиксов и 3 фразы; конкретные фразы, которые он пропускает | §4.1 |
| экранный отчёт 65 396 знаков против 143 463 в PDF | §4.3 |

**Оценочное, а не измеримое:** вся таблица «хуже / так же / лучше» в §3 и разбор §3.1–§3.4 — это
редакторское суждение по каждому из 235 текстов, а не метрика. Спорить с ним нормально; проверять
запуском нечего.

Две ошибки в числах уже были найдены на третьем проходе и исправлены здесь: `D↔социальное` — 72 из
86, а не 68; `repeatedSummary` — 1 110 знаков, а не 1 173. Это же причина, по которой отчётные числа
из таблицы выше стоит перемерять.

### Рабочие адреса всех 20 разделов

Матрица 31.03.1993 (`4-3-22`). Все 20 проверены — отдают 200; на них воспроизводятся дефекты §1.1,
§1.2, §1.3, §1.5 и §2.2.

| Раздел | Адрес |
|---|---|
| character | `/encyclopedia/character/4-3-22` |
| comfort | `/encyclopedia/comfort/4-15-7` |
| profession | `/encyclopedia/profession/3-10-7` |
| realisation | `/encyclopedia/realisation/11-22-8` |
| karma40 | `/encyclopedia/karma40/15-8` |
| resources | `/encyclopedia/resources/8-13` |
| family_gifts | `/encyclopedia/family_gifts/7-7-13-22` |
| soul_tasks | `/encyclopedia/soul_tasks/3-11-14` |
| past_lives | `/encyclopedia/karmic-tail/15-8-11` |
| purpose | `/encyclopedia/purpose/22-8-3-11` |
| money | `/encyclopedia/money/8-13-5-8` |
| money40 | `/encyclopedia/money40/13-8` |
| relations | `/encyclopedia/relations/15-20-5-7` |
| parents_children | `/encyclopedia/parents_children/7-7-6` |
| ancestry | `/encyclopedia/ancestry/15-13-22-11` |
| body_resource | `/encyclopedia/body_resource/22-11-6` |
| chakras | `/encyclopedia/chakras/4-3-7-12-10-22-8-7-15-12-11-5-4-4-8-8-15-5-22-11-6-7-7-14` |
| rest | `/encyclopedia/rest/5-4` |
| loops | `/encyclopedia/loops/11-4-3` |
| years | `/encyclopedia/years/4-7-3-7-22-6-11-15?birth=1993-03-31` |

18 из 20 разделов платные: чтобы увидеть их в отчёте и в PDF, надо зарегистрировать **свою
уникальную** почту и оплатить мок-провайдером. Бесплатных разделов два — `character` и `comfort`.

### Sitemap и robots на стенде не отдаются

`web/app/sitemap.ts` возвращает пустой список вне `https://arcana-sense.ru`; `robots.txt` на стенде —
`Disallow: /`. Состав карты проверяется прогоном самой функции `sitemap()` с подменённым
`NEXT_PUBLIC_SITE_URL` (тем же внешним конфигом vitest) — 356 адресов.

---

## Что итерация 3 сделала правильно

Это не надо трогать и не надо перепроверять заново:

- **все 18 чисел достижимых результатов сходятся до единицы** (проверено независимым перебором
  `matrices.json`, не по числу в тесте): comfort 286 (260/12/14), profession 160 (151/7/2),
  realisation 86, karma40 332, resources 95, family_gifts 4463, soul_tasks 239, purpose 42,
  money 1766, money40 95, relations 1685, parents_children 4429, ancestry 886, body_resource 363,
  chakras 5544, rest 215, loops 54, years 5544;
- **160 заголовков глав и 100 вопросов FAQ уникальны** дословно и после подстановки названия
  раздела; 0 общих 8-словных шинглов между любыми двумя разделами;
- все 20 общих статей в боевом sitemap, персональных разборов там ноль;
- все 20 персональных маршрутов: `noindex, follow` + self-canonical + `Article` + `BreadcrumbList`
  + крошки через «Энциклопедия»; штатный 404 на недостижимом и мусорном слаге;
- отчёт, PDF и энциклопедия берут итог из одного `web/lib/sections.ts::build`;
- две ссылки из раздела — обычной текстовой ссылкой без нового CTA (требование `AGENTS.md`);
- publication registry кармических хвостов: 26 статей, 22 индексируются, 4 закрыты, sitemap согласован;
- `seo.title` / `description` / `queries` не испорчены ни в одном из 8 изменённых файлов.

---

## 1. Блокирующее: видимый брак в тексте продукта

### 1.1. Двойная точка на 15 194 персональных страницах

`variantRoleTemplate` отдаёт `strength` и `risk` уже с точкой
(`web/lib/roleContent.ts:204-205`), а шаблоны итога (`web/lib/sectionReadingShared.ts:261-262`) и
связей (`web/lib/sectionReadings.ts:458`) добавляют свою. Получается «…когда человек держит ровный
темп и не выгорает**..**». Уходит в энциклопедию, в отчёт и в PDF.

Замерено по всем 26 284 разборам: дефект в 9 разделах из 18.

| Раздел | Страниц с `..` |
|---|---|
| `years` | 5544 из 5544 |
| `family_gifts` | 4463 из 4463 |
| `money` | 1766 из 1766 |
| `relations` | 1685 из 1685 |
| `ancestry` | 886 из 886 |
| `body_resource` | 363 из 363 |
| `soul_tasks` | 239 из 239 |
| `profession` | 153 из 160 |
| `resources` | 95 из 95 |

- [x] Договориться, кто ставит финальную точку: либо кубик роли отдаёт текст без точки и её ставит
  шаблон, либо шаблон не добавляет свою. Починить в одном месте, а не подчисткой строк.
- [x] Закрыть тестом: прогон по всем достижимым слагам всех 18 разделов на `/\.\.(?!\.)/` и `/\.;/`
  в `summary`, `strength`, `tension`, `practice` и абзацах связей.

**Сделано.** Конвенция: `strength` и `risk` — придаточные **без** финального знака, `essence` и
`action` — законченные предложения с точкой. Она выбрана потому, что так их уже читало большинство
шаблонов (`comfortReading`, `characterTypes`, `combinationReading`, оба итога), а расходились только
три источника.

Правки:

| Файл | Что |
|---|---|
| `web/lib/roleContent.ts` | добавлен `clause()`, снимающий финальные `.` и `;`; применён в `positionRoleTemplate` (обе ветки, включая `directParts`) и в `variantRoleTemplate` |
| `web/lib/sectionReadingShared.ts:217` | `${middle.risk} В итоге` → `${middle.risk}. В итоге` |
| `web/lib/sectionReadings.ts:497-498` | точка после `${target.strength}` и `${target.risk}` в синтезе |
| `web/lib/sectionReadings.ts:730-731` | точка после `${current.strength}`; `${current.risk}. Возрастная рамка` |
| `web/lib/sectionReadings.test.ts` | расширен существующий обход всех достижимых результатов — проверка на `..`, `.;`, `,,`, пробел перед знаком, `::`; таймаут поднят до 30 с (обход 26 284 разборов) |

Проверено: замер по всем 26 284 разборам — **0 страниц с `..` и `.;`** (было 15 194). Тест
перепроверен на возврате дефекта: краснеет с сообщением `resources:10-5: сдвоенный знак «…по делу..»`,
после восстановления правки зеленеет. Полный прогон: web unit 350/350, typecheck ✓,
`pytest engine content` 62/62, `content.validate` 5916 проверок / 0 ошибок.

Побочно закрылась часть §2.2: у `profession` пропала точка внутри кавычек — было
«опирается на принцип «…всё сезонное.», а в результате», стало «…всё сезонное», а в результате».

### 1.2. Дисклеймеры безопасности исчезают при повторе аркана — 2 190 разборов

`repeatedSummary` (`web/lib/sectionReadingShared.ts:180-196`) полностью замещает предметный
`summary` (`:260`) общим текстом «В последовательности X–Y–Z тема N звучит 2 раза…», одинаковым для
всех разделов. Вместе с ним со страницы уходит единственное предупреждение раздела.

Замерено:

| Раздел | Что пропадает | Результатов |
|---|---|---|
| `ancestry` | «формула не считает семь отдельных поколений и не говорит о „проклятиях“» | **535 из 886 (60,4 %)** |
| `money` | «не прогнозирует сумму или дату дохода» | 446 из 1766 (25,3 %) |
| `relations` | «Это не совместимость двух дат и не прогноз брака» | 423 из 1685 (25,1 %) |
| `parents_children` | «независимо от того, есть ли у человека дети» | 733 из 4429 (16,6 %) |
| `body_resource` | «не заключение о состоянии организма» | 24 из 363 |
| `karma40` | «не обещает событие в день сорокалетия» | 20 из 332 |
| `loops` | «„программой“ только как часть рассчитанного раздела» | 5 из 54 |
| `resources` · `money40` | «не обещает богатства» · про сорокалетие | 2 из 95 · 2 из 95 |

Под удар попадает и эталонная дата 31.03.1993: у неё `parents_children` = `7-7-6`, дисклеймера про
детей в отчёте нет.

- [x] Сюжет повтора **добавлять** к предметному итогу раздела, а не замещать его.
- [x] Закрыть тестом: для каждого раздела с дисклеймером прогнать все достижимые слаги и
  потребовать, чтобы контрольная фраза была в `summary` у 100 % результатов.

**Сделано.** Приём `repeated ?? базовый текст` повторялся в трёх местах и везде вытеснял предметный
итог. Заменён на добавление: базовый текст, затем сюжет повтора.

| Файл | Что |
|---|---|
| `web/lib/text.ts` | добавлен `withRepeat(base, repeated)` — общий для обоих строителей итога, без циклического импорта |
| `web/lib/sectionReadingShared.ts` | ветка `profession` и общий возврат переведены на `withRepeat` |
| `web/lib/comfortReading.ts` | `buildComfortConclusion` переведён на `withRepeat` |
| `web/lib/sectionReadings.test.ts` | в тот же обход добавлена карта `guards` и проверка, что оговорка раздела есть в `summary` у 100 % результатов |

Проверено замером по всем достижимым слагам: **0 разборов без оговорки** (было 2 190). Число
страниц, где сюжет повтора выводится, в точности совпало с прежним числом пропаж — 446 `money`,
423 `relations`, 733 `parents_children`, 535 `ancestry`, 24 `body_resource`, 20 `karma40`,
9 `profession`, 5 `loops`, по 2 `resources` и `money40` — то есть текст добавился, а не заменил.
Тест перепроверен на возврате дефекта: краснеет с `karma40:10-10: из итога пропала оговорка
«в день сорокалетия»`. Полный прогон: web unit 350/350, typecheck ✓, `pytest engine content` 62/62,
`content.validate` 0 ошибок.

Сверх плана закрылся `profession`: его оговорка «не список обязательных профессий» тоже пропадала
на 9 линиях с повтором — в таблице выше этого раздела не было.

### 1.3. Треть написанного текста ролей не доходит до страниц — 588 из 1 820 предложений

`positionRoleTemplate` (`web/lib/roleContent.ts:30-55`) и `variantRoleTemplate` (`:198-207`) берут
только **первое и последнее** предложение позиционного текста, а «силу» и «риск» подставляют из
общих `plus[0]` / `minus[0]` аркана. Средние предложения выбрасываются. Исключение одно — роль P
(`directParts: true`).

Замерено по 27 ролевым позициям × 22 аркана: 1 820 предложений в корпусе, используется 1 232,
**выброшено 588 (32 %)**. Больше всего теряют `money` и `family_gifts` (по 30), `resources` (29),
`relations` (25), `rest` (24).

Прямое следствие для пункта §20.2 итерации 3: специально написанное «В сильном прохождении человек
переводит идею в первый проверяемый результат; в риске набирает много стартов и теряет интерес до
завершения» на странице **отсутствует**. Проверено на
`/encyclopedia/years/4-7-3-7-22-6-11-15?birth=1993-03-31`: вместо него «Сильное прохождение периода
заметно, когда человек **держит слово и сроки**» — это `plus[0]` Императора, общий для всех его
ролей в любом разделе.

То же по §6: у `resources` написаны 22 текста R2 с блокировкой, а на странице вместо блокировки
общий `minus` аркана. Фразы «не держаться из страха за то, что уже кончилось» нет ни на
`/encyclopedia/resources/8-13`, ни в `money`, ни в `money40` — только на `/encyclopedia/arcanum/13`.

- [x] Разбирать позиционный текст на все его предложения, а не на первое и последнее.
- [x] «Силу» и «риск» брать из позиционного текста там, где они там написаны; общий `plus`/`minus`
  оставить запасным вариантом для позиций, где четвёртого предложения нет.
- [x] Закрыть тестом: для каждой ролевой позиции ни одно предложение корпуса не должно оставаться
  неиспользованным.

**Сделано.** Сначала замерена форма корпуса — она оказалась неоднородной, и от этого зависело, что
вообще можно взять из текста:

| Форма | Сколько текстов | Что берём |
|---|---|---|
| 4 предложения | 50 | суть / сила / риск / действие написаны раздельно — берём как есть |
| 3 предложения с маркером риска в середине («…; в риске …», «…; риск — …») | 24 (21 `years`, 3 `body_resource`) | середина делится на силу и риск, зачин «В сильном прохождении человек» срезается, предикат подставляется в рамку роли |
| 3 предложения без маркера | 527 | середина не делится, но и не выбрасывается: уходит в суть, сила и риск остаются из `plus`/`minus` |
| 2 предложения | 14 | суть и действие, середины нет |

Правки в `web/lib/roleContent.ts`: общий разбор `splitPosition()` вместо двух расходящихся; флаг
`directParts` удалён — четырёхчастность определяется по самому тексту, а не вручную (из-за флага
28 из 50 написанных наборов не использовались). Убраны лишние аргументы в
`sectionReadings.ts` и `combinationReading.ts`.

Проверено: **0 потерянных предложений из 2299** (было 588 выброшенных). Силу и риск из собственного
текста берут 94 роли вместо 22. На живом примере — `years` аркан 1: было «Сильное прохождение
периода заметно, когда человек **двигается быстрее всех к понятной цели**» (общий `plus` Колесницы),
стало «…когда человек **переводит идею в первый проверяемый результат**» — то самое предложение,
ради которого писался §20.2 итерации 3. Блокировка ресурса («Пока помощь бесплатна из принципа,
забота остаётся невидимой работой») тоже теперь на странице.

Тест `не теряет ни одного предложения позиционного корпуса` добавлен в `sectionReadings.test.ts`;
перепроверен на возврате дефекта — краснеет, перечисляя 488 потерянных предложений.

**Два решения по ходу, которые стоит знать:**

1. **Рамка роли сохранена всегда.** Первая версия подставляла написанное предложение вместо рамки —
   и существующий тест поймал, что «итог М» и «итог Ж» (а также `R1`/`R`, `R`/`земля`) стали
   дословными копиями: они читают одну позицию корпуса. Написанное целым предложением уходит в суть,
   в рамку подставляется только предикат. Парные роли различаются на 22/22 арканах во всех четырёх
   парах — замерено. Полностью взять силу и риск из текста для этих ролей можно будет только после
   решения по §2.1 (написать отдельные тексты на роль).
2. **Порог длины кубика в тесте снижен с 40 до 12** — как у роли P в том же файле. Порог 40 мерил
   не содержательность, а длину префикса рамки; теперь кубик может идти своим коротким текстом:
   `resources` R2 аркан 3 — «Условие — брать за это деньги» (30 знаков), и это лучше прежней общей
   фразы. Минимум по всему корпусу — 12 знаков.

Прогон: web unit **350 из 351**, typecheck ✓, `content.validate` 0 ошибок. Единственное падение —
`lib/matrix.parity.test.ts` и его python-двойник: снимок паритета датирован `2026-09-01`, наступило
2 сентября, тест требует `npm run golden`. К правкам отношения не имеет, эталон я не пересобирал.

### 1.4. Неверный пример в статье `/encyclopedia/position/realisation`

Глава «Пример пути 16–11–3» утверждает: «В контрольной матрице 4–3–22 путь самореализации
складывается как 16–11–3». Движок для 4–3–22 даёт **11–22–8**; `/encyclopedia/realisation/16-11-3`
отдаёт **404** — такая тройка недостижима вообще. Названы и не те арканы (Башня/Справедливость/
Императрица вместо Справедливость/Шут/Сила). Врезка на той же странице ведёт на `11-22-8`, то есть
статья противоречит сама себе.

Источник: `tools/seo/content/positions/realisation.json` → `article_sections`, глава примера.

- [x] Исправить числа и названия арканов, пересобрать контент.
- [x] Закрыть проверкой: все числовые примеры в статьях сверять с движком — тройка из текста должна
  быть достижима и совпадать с расчётом названной в тексте матрицы. Сейчас `content/validate.py`
  примеры не сверяет ни с чем.

**Сделано.** Глава переписана под настоящий расчёт в
`tools/seo/content/positions/realisation.json`, контент пересобран
(`python ../../tools/seo/build-content.py`) — в `web/content/positions.json` изменились ровно две
строки:

> Пример пути **11–22–8**. «В контрольной матрице 4–3–22 путь самореализации складывается как
> 11–22–8. Справедливость в D возвращает один и тот же сюжет про перекос: его придётся называть
> вслух, а не терпеть до срыва. Шут на личном уровне требует другого — разрешить себе начать заново,
> без старого груза и без страха потерять лицо. Сила на социальном превращает этот опыт в спокойную
> опору для других: рядом с таким человеком выдерживают там, где обычно срываются.»

Сторож `check_examples` добавлен в `content/validate.py`: для каждой статьи раздела числа из
заголовка примера сверяются с тем, что даёт `engine.sections` для контрольной матрицы `4-3-22`.
Возрастные рамки `years` («0–10 лет») под правило не попадают — у них другое количество чисел, чем
ролей в разделе. Проверок стало 5922 вместо 5916. Перепроверено на возврате дефекта: краснеет
сообщением «позиция realisation: пример в главе 6 назван 16–11–3, а движок для матрицы 4-3-22 даёт
11–22–8».

**Заодно проверены примеры всех остальных статей** — расхождений больше нет: `character` 4–3–22,
`comfort` 4–15–7, `profession` 3–10–7, `resources` 8–13, `soul_tasks` 3–11–14 совпадают с движком.

### 1.5. Горизонтальная прокрутка карты чакр на узких экранах

Замерено на `/encyclopedia/chakras/4-3-7-12-10-22-8-7-15-12-11-5-4-4-8-8-15-5-22-11-6-7-7-14`
(playwright, `documentElement.scrollWidth − clientWidth`):

| Ширина | 320 | 360 | 390 | 768 | 1366 |
|---|---|---|---|---|---|
| `scrollWidth − clientWidth`, px | **57** | **17** | 0 | 0 | 0 |

Виновата последняя хлебная крошка с 24-компонентным слагом: `.crumbs .crumb { white-space: nowrap }`
(`web/app/globals.css:1487`), а правило усечения не срабатывает, потому что `.enc-crumbs .crumb` —
`inline-flex` (`:1396`), и `max-width: 100%` у внутреннего `span` считается от собственной ширины.

Отдельно сама таблица: `table-layout: fixed` берёт ширины из `thead`, поэтому все колонки по 280 px
при видимой области 234/274 px — на 320 и 360 px ни одна колонка не читается целиком; правило
`.personal-chakras tbody th { width: 190px }` (`:375`) мёртвое.

Почему не поймали: `e2e/test_section_readings.py` меряет только 390 и 1366 px.

- [x] Дать последней крошке усечение (или показывать вместо 24 чисел короткую подпись).
- [x] Разобраться с шириной колонок таблицы на 320/360 px.
- [x] Расширить браузерную проверку переполнения на 320 и 768 px для всех 20 персональных маршрутов.

**Сделано.** Причина оказалась не в правиле усечения, а в том, что оно физически не могло сработать:
`.crumb` — флекс-элемент, он не сжимается ниже содержимого, пока у него `min-width: auto`, поэтому
`max-width: 100%` у внутреннего span считался от уже раздутой ширины. Добавлено `min-width: 0` на
последний шаг и на его текстовый span (`web/app/globals.css`, медиазапрос ≤ 640 px).

Замер на живом стенде до и после:

| Ширина | 320 | 360 | 390 | 768 | 1366 |
|---|---|---|---|---|---|
| было | **57** | **17** | 0 | 0 | 0 |
| стало | 0 | 0 | 0 | 0 | 0 |

Крошка теперь усекается многоточием: 269 px видимой части из 346 px содержимого. Проверено ещё на
трёх маршрутах (`years`, `family_gifts`, `soul_tasks`) — везде 0.

**По ширине колонок таблицы принято решение не менять раскладку.** Таблица лежит в контейнере с
`overflow-x: auto` (замерено: 234 px при 320 px экрана) — горизонтальная прокрутка внутри таблицы
это штатный паттерн, а прокрутка всей страницы устранена. Уместить в 320 px колонку-заголовок и
столбец с абзацем текста без потери читаемости нельзя. Что исправлено: правило
`.personal-chakras tbody th { width: 190px }` было **мёртвым** — при `table-layout: fixed` ширины
берутся из первой строки, то есть из `thead`, и все четыре колонки выходили по 280 px. Ширина
перенесена на `thead th:first-child`, как и задумывалось.

Браузерная проверка `test_all_20_personal_article_layouts_fit_mobile_and_desktop` расширена с двух
ширин до пяти — 320, 360, 390, 768, 1366 px × 20 маршрутов. Прогон зелёный.

### 1.6. Красный тест в релизном наборе

`e2e/test_character.py::test_combination_page_contains_all_section_contexts` падает
(`1 failed, 3 passed`). Тест добавлен этой же итерацией и ждёт на `/encyclopedia/combination/3-4`
заголовки контекстов E↔M и B↔P. Перебор `matrices.json` показывает: для пары {3,4} достижим только
E↔K. **Страница отдаёт правильно, сломан сам тест.**

- [x] Переписать тест на пару, у которой нужные контексты реально достижимы, либо строить ожидания
  из того же источника достижимости, что и страница.

**Сделано.** Перебором `matrices.json` найдено, что пар с достижимыми всеми девятью контекстами
ровно семь: (3, 11), (6, 9), (6, 10), (6, 16), (6, 18), (10, 15), (12, 18). Тест переведён на **3 и
11** и теперь проверяет не подмножество, а все три группы и все девять контекстов сразу — покрытие
шире прежнего. Порядки пары не перечисляются поимённо (печатаются только достижимые): проверяется,
что напечатан хотя бы один. Прогон `e2e/test_character.py` — 4 из 4 зелёные.

### 1.7. PDF отчёта в 7 pt от жёсткого предела высоты

- [x] Разбить отчёт на несколько страниц PDF либо ввести проверку высоты с запасом.
- [x] Добавить в набор проверку «в PDF присутствуют все 20 разделов», устойчивую к сломанному
  ToUnicode (извлечение PyMuPDF, а не `pdftotext`).

**Проверено — исходная формулировка пункта неверна, обрыва нет.** Число 14 393 pt подтвердилось
(купил моком, скачал PDF, `pypdf`: одна страница 271 × 14 393 pt), но вывод «запас 7 pt, любой
абзац обрежет документ» ошибочен. Сервис печати сам укладывается в предел:
`compose/browser/server.mjs::onePage` считает `scale = min(1, 19 190 / высота в px)` и сжимает лист.
По логам печати: `лист 1280×68443 px → 19190 px, масштаб 0.280`. То есть 14 393 pt — это не край
случайности, а расчётный максимум, в который документ приводится всегда. В коде есть и комментарий
про уже исправленный когда-то обрыв на 14 428 pt.

Настоящее следствие роста отчёта мягче и другое: **лист не обрезается, а сжимается сильнее** —
сейчас текст печатается в 0,28 натуральной величины, и каждый новый раздел уменьшает кегль. Это
вопрос читаемости, а не потери данных.

Что сделано в `e2e/test_method_release.py`:

1. **Оракул переведён с `pdftotext` на `pypdf`.** По ходу правок тест упал на «в PDF нет „Средняя
   точка хвоста“». Проверил оба извлекателя на одном файле: `pdftotext` — 99 665 знаков и строки
   нет, `pypdf` — 151 530 знаков и строка есть. Текст в PDF был; терял его извлекатель. Мои правки
   §1.3 удлинили отчёт и сдвинули границу, за которой ломается ToUnicode, — латентная хрупкость
   теста стала видимой.
2. **Добавлена проверка состава целиком**: все 20 заголовков из `spec/sections.json` должны
   присутствовать в извлечённом тексте (замер на живом PDF — 20 из 20).
3. **Добавлена проверка формата**: ровно один лист и высота не выше 14 400 pt, с комментарием, что
   уход масштаба ниже четверти — сигнал делить разбор на страницы, а не сжимать дальше.

Прогон `e2e/test_method_release.py` — зелёный.

**Решение владельца: страницы не нужны, постраничная печать отклонена.** Файл покупают, чтобы
читать с экрана, и для этой задачи он не сломан. Разбор по пунктам:

- **разбить на листы нечем без потери качества.** Chromium ломает поток там, где кончился лист, —
  посреди карточки роли и посреди таблицы чакр. `break-inside: avoid` меняет разрывы на пустоты:
  карточка, не влезающая в остаток листа, уезжает целиком. Третьего варианта нет, и одним листом
  сделано именно поэтому. Ветка `format: "A4"` в службе печати существует
  (`compose/browser/server.mjs`, флаг `SINGLE_PAGE`), но включать её значит выбрать между
  разорванными блоками и пустотами;
- **мера «1,4 мм на знак» — бумажная, а задача экранная.** В файле текст остался текстом:
  `pypdf` вытаскивает 149 681 знак, то есть это вектор. На экране подгонка по ширине показывает
  лист 9,5 см целиком, а увеличение ничего не портит;
- **что остаётся:** масштаб падает с ростом разбора (было 1,0, сейчас 0,28, лист стоит в 7 pt от
  потолка формата). Последствие ровно одно — печать на бумаге, которой нет. Контент при этом не
  теряется: сжимается кегль, а не состав.

- [x] **Постраничная вёрстка печати — отклонена, а не перенесена.** Сторожем остаётся то, что уже
      стоит в `e2e/test_method_release.py`: ровно один лист, высота не выше предела формата,
      все 20 заголовков из `spec/sections.json` внутри файла. Если разбор когда-нибудь удвоится,
      выход не в страницах, а в разрезе по границам разделов: несколько длинных листов, каждый
      кончается там, где кончился раздел — ни разрывов внутри блоков, ни пустот.

**Тот же корень нашёлся ещё в одном тесте.** `e2e/test_known_bugs.py::test_a11_pdf_keeps_the_footer`
искал «arcana-sense.ru» через `pdftotext` и падал, хотя подпись в файле есть: проверено на двух
разных PDF — `pdftotext` не находит, `pypdf` находит. Измерение переписано так же, лишний импорт
`subprocess` убран. Оба теста зелёные.

---

## 2. Пункты плана, закрытые формально

### 2.1. «Два разных ролевых модификатора» различают только рамку

Требования §6, §7, §11, §13, §15 итерации 3: «22 результата R1 и 22 R», «четыре кубика мужской и
женской ролей различаются для каждого аркана», «развести 44 результата между собой».

Дословно строки различаются — поэтому автотест `expect(first[part]).not.toBe(second[part])`
(`web/lib/sectionReadings.test.ts:235`) проходит. Но предметное тело у парных ролей одно: рамка
меняет вводную клаузу, а суть, действие и `plus[0]`/`minus[0]` берутся из одной позиции.

Замерено — общий смысловой хвост (≥ 40 знаков) из 22 арканов:

| Пара ролей | essence | strength | risk | action |
|---|---|---|---|---|
| `family_gifts`: итог М / итог Ж | 22 | 22 | 22 | 22 |
| `ancestry`: задача М / задача Ж | 22 | 19 | 22 | 22 |
| `relations`: R1 / R | 20 | 19 | 22 | 22 |
| `money`: R / итог земли | 22 | 19 | 22 | 22 |

Пример (аркан 5): риск «итога М» и «итога Ж» — одна фраза «…когда человек читает лекции там, где
просили помощи». У `family_gifts` есть 2 743 из 4 463 результатов, где итог М численно равен итогу Ж —
там два блока «дар» просто копии друг друга.

То же в чакрах: «66 модификаторов аркан × колонка» фактически 22 — один `plus[0]`/`minus[0]` и одно
действие во всех трёх колонках, схожесть строк 0,93.

- [x] Решить: либо писать роли отдельными текстами, либо переписать формулировку требования в плане
  и не заявлять «22 результата» там, где их 22 на две роли.
- [x] Если оставляем рамки — сторожевой тест должен сравнивать **предметную часть** (текст без
  рамки), а не строку целиком.

**Решение: не писать 88 новых текстов, а убрать дубль там, где он виден.** Основание — замер: копия
возникает не всегда, а только когда парные роли выпадают на **один аркан**. При разных арканах
тексты позиции разные, и никакой копии нет.

| Пара | Одинаковый аркан |
|---|---|
| `family_gifts`: итог М = итог Ж | **2 743 из 4 463 (61,5 %)** |
| `ancestry`: задача М = задача Ж | **431 из 886 (48,6 %)** |
| `relations`: R1 = R | 133 из 1 685 (7,9 %) |
| `money`: R = земля | 89 из 1 766 (5,0 %) |

Весь ущерб сосредоточен в двух разделах и в вырожденном случае. Отдельные корпуса на роль — это
редакционный проект на 88 текстов, а не правка дефекта; он остаётся возможным, но не обязателен.

**Что сделано.** Роль-повтор помечается полем `sameAs` (`web/lib/readingTypes.ts`), разбор печатает
вместо второй копии отсылку: «Тот же аркан, что и в роли „Дар мужской ветви“: разбор выше. Обе
позиции формулы дали одно число, поэтому второй раз тот же текст не повторяется». Из итога
(`buildSectionConclusion`) такая роль исключена — раньше он выдавал «дар мужской ветви: X; дар
женской ветви: X».

**Признак пары задан явно, а не выведен структурно** — и это выяснилось замером, а не рассуждением.
Первая версия искала повтор по «одна позиция + один аркан» и зацепила 5 018 карт чакр и 3 176
возрастных линий: там семь уровней и восемь десятилетий делят позицию корпуса, и одинаковый аркан
означает разные периоды, а не повтор текста. Вторая версия («разные модификаторы одной позиции»)
всё ещё цепляла чакры: «физика уровня» и «итог физики» тоже разные модификаторы. Поэтому в
`SECTION_ROLES` добавлено поле `pairedWith` ровно у четырёх ролей. После этого схлопываются ровно
нужные 2 743 / 431 / 133 / 89 — числа сошлись с независимым перебором `matrices.json`.

**Сторожевой тест переписан.** Прежний требовал «четыре кубика различаются» и проходил от одной
рамки — предметная часть у парных ролей совпадает по построению. Новый проверяет то, что защищает
читателя: при одном аркане вторая роль помечена повтором и не печатается дважды, при разных — обе
полноценные и `essence` у них различаются. Обе ветки обязаны встречаться, иначе проверка вырождается
в одну сторону.

Прогон: web unit **357 из 358** (падает только календарный паритет), typecheck ✓,
`content.validate` 0 ошибок.

**Формулировку требования в плане итерации 3 надо поправить** при следующем пересмотре: «22 итога
мужской и 22 итога женской ветви» — это 22 текста на пару ролей, а не 44. Архитектура «общий корпус
арканов + ролевой модификатор» другого и не даёт.

### 2.2. Итог 14 разделов из 18 — механическая склейка

Правило раздела 1 требует итог «без механической склейки трёх абзацев». Вручную написаны только
`character` и `comfort` (у `chakras` и `years` своя раскладка). Остальные 14 собираются
`items.map(...).join("; ")` (`web/lib/sectionReadingShared.ts:225-262`).

Как это выглядит на `/encyclopedia/soul_tasks/3-11-14`:

> Согласованный вариант заметен так: первая задача неба: создаёт тепло и уют вокруг себя; вторая
> задача неба: видит перекос и называет его прямо; общий урок: Общий урок проживается зрело, когда
> человек держит ровный темп и не выгорает..

Для сравнения `comfort`, где итог написан:

> Опора тройки появляется, когда в базовом состоянии человек держит слово и сроки, в первой реакции
> умеет хотеть и добиваться материального, а для возвращения к себе двигается быстрее всех к
> понятной цели.

Побочные следствия склейки:
- заглавная буква и точка внутри придаточного: `profession` — «…опирается на принцип «Хорошо там,
  где рынок движется: новые направления, посредничество, торговля, всё сезонное.», а в результате…»
  (все 160 страниц);
- грамматический брак в `family_gifts`: «…когда в позиции итог М человек **Дар превращается в
  семейную обязанность**…» — 1 720 из 4 463 (`web/lib/sectionReadings.ts:459`);
- дословный повтор клаузы при повторе аркана: `character` 724 из 5 544, `comfort` 26 из 286,
  `realisation` 18 из 86, `karma40` 20 из 332;
- рассогласование падежей: `soul_tasks` — «Затем действие итога **итог неба**:».

- [x] Убрать вставку целых предложений внутрь придаточных. **Сделано** — но не так, как
  предполагал план: кубики остались двух форм, а обязанность различать их переехала к потребителю.
  `web/lib/text.ts` получил `cubeClause`: придаточное получает подлежащее («человек держит слово и
  сроки»), готовое предложение отдаёт заглавную букву («ресурс сохраняется, когда человек…»). Обе
  формы теперь встают в одну цепочку, и рамку вариантной роли снимать не пришлось.
- [x] Роли вставляются обстоятельствами, а не перечислением через точку с запятой. **Сделано** в
  шести местах вставки: связи и синтез (`sectionReadings.ts`), итог общего вида и итог `profession`
  (`sectionReadingShared.ts`), статьи сочетаний (`combinationReading.ts:299-300`), возрастной блок
  `years`. Точка с запятой осталась только разделителем ролей, а двоеточие перед кубиком заменено
  на тире, чтобы не давать двойное двоеточие внутри фразы.

**Замер до и после (все 26 284 достижимых разбора плюс 231 статья сочетаний):**

| Дефект | до | после |
|---|---|---|
| «рамка на готовом предложении» — «в позиции итог М человек **Дар превращается в…**» | **11 458** | **0** |
| «человек Загл» в статьях сочетаний (231 статья) | есть на всех разделах с `profession` | **0** |
| двоеточие с заглавной внутри фразы («условие: Талант…») | 2 027 в выборке 2 278 разборов | **0** |

Сторож поставлен в существующий обход всех разборов
(`web/lib/sectionReadings.test.ts:489` — «рамка на готовом предложении»): на прежнем коде даёт
11 458 нарушений, на новом — ноль. Прогон: 22 теста файла зелёные за 10,1 с.

Дополнительно убраны два оборота, которые всплыли на образцах:
- **двойное «действие»** — «Сначала проверьте действие 0–10: Подготовить переход к следующему этапу
  помогает действие: Выпишите…». Рамка действия снята с кубика у всех 14 вариантных ролей
  (`VARIANT_FRAMES` больше не хранит `action`): роль уже названа в сути, и второй родительный
  подряд ничего не добавлял;
- **тройное «когда»** — «Напряжение заметно, когда в позиции 0–10 риск десятилетия усиливается,
  когда человек бросает на середине, когда пропал азарт». `pairCubes` сворачивает общий зачин двух
  ролей одного вида (восемь десятилетий, колонка чакр) до подлежащего: «в позиции 0–10 — человек
  бросает на середине, когда пропал азарт; в позиции 10–20 — человек молчит там, где нужно сказать
  прямо».

**Разобрано, но не сделано: это перестройка, а не правка.** Замер: склейкой собран итог у
**14 разделов** — `realisation`, `karma40`, `resources`, `family_gifts`, `soul_tasks`, `purpose`,
`money`, `money40`, `relations`, `parents_children`, `ancestry`, `body_resource`, `rest`, `loops`.
Свой итог у `comfort`, `profession`, `chakras`, `years`.

Почему нельзя просто заменить `join("; ")` на обстоятельства, как в `comfort`: **кубики ролей
разной грамматической формы**. У обычных ролей `strength` — предикат к «человек» («держит слово и
сроки»), у вариантных — законченное предложение с рамкой («Ресурс сохраняется, когда человек не
держится за мёртвое»). В одну цепочку «в такой-то роли человек …» они не встают: получится «в роли
R2 Ресурс сохраняется, когда человек…».

Чтобы итог читался как у `comfort`, кубики должны быть однородными предикатами — то есть рамку надо
снять с кубика и оставить только в подписи роли и её вопросе, где тот же смысл уже есть («R2 ·
Условие удержания ресурса»). Раньше рамка держала на себе и различение парных ролей, но после §2.1
это делает `sameAs`, так что она больше не обязательна. Останется вопрос, не теряется ли с рамкой
смысл «что именно означает сила в этой роли».

- [x] Решить: снимать ли рамку с кубиков вариантных ролей. **Решение: не снимать со силы и риска,
      снять с действия.** Рамка силы и риска несёт смысл роли («Ресурс сохраняется, когда человек…»
      объясняет, что именно значит сила в точке R2), и на карточке роли она читается как обычная
      фраза — портится не карточка, а вставка кубика в чужое предложение. Дешевле научить шесть
      потребителей различать две формы (`cubeClause`), чем переписывать 698 кубиков и терять
      объяснение роли. Рамка действия снята: там она добавляла только второе «действие» подряд.

### 2.3. Мёртвый и неподключённый контент

**Формулировка «не рендерятся нигде» неверна — проверено.** Страница аркана печатает **все 37**
позиций корпуса: `web/app/encyclopedia/arcanum/[n]/page.tsx:118` обходит `Object.entries(e.inPositions)`
целиком, а в артефакте `web/content/arcana.json` у каждого аркана ровно 37 ключей. То есть 154
«мёртвых» текста опубликованы на 22 индексируемых страницах — они не участвуют в **разборе раздела**,
но не невидимы. Требования валидатора к ним обоснованы, и снимать их нельзя: это публикуемый текст.

- [x] Решить по каждому корпусу: подключить к странице раздела или удалить. **Решение принято по
      факту публикации**: `character`, `comfort`, `karma40`, `loops`, `purpose`, `realisation`
      остаются как тексты страницы аркана — они отвечают на вопрос «что этот аркан значит в такой
      позиции», а разбор раздела собирает свои роли из точек, которые считает формула. Удалять
      публикуемый текст незачем, подключать в разбор — значит дублировать роли, которые там уже есть.
      Один корпус из списка подключён по-настоящему: см. следующий пункт.
- [x] Если корпус остаётся неиспользованным осознанно — снять с него требования валидатора.
      **Отменено проверкой**: тексты публикуются, поэтому проверки уникальности и политики к ним
      применимы. Ложного ощущения покрытия нет — есть ошибка в разборе.
- [x] **`money40`: раздел переведён на собственный корпус.** Спецификация
      (`spec/sections.json:262-271`) требует `position_key: "money40"` для роли R2, а код читал
      `position: "resources"` — раздел «Как меняются деньги после 40 лет» собирался из текстов
      «Блага и ресурс». Причина, по которой так было: все 22 текста `money40` написаны в **два**
      предложения, а движку нужно минимум три.

      Что сделано: 22 текста доведены до четырёх предложений (суть / сила / риск / действие),
      снято всё хеджирование — «может/могут/можно» было 15, стало **0**; добавлены рамки роли
      `money_mature` и `money_entry_mature`; роль R2 переведена на позицию `money40`, роль L
      получила собственную рамку. Проверка корпуса: 22 из 22 по четыре предложения, длина
      366–485 знаков, служебных каркасов нет, запрещённых формул нет, дублей предложений и первых
      30 знаков по всему корпусу (37 × 22) нет.

### 2.4. Отдельные требования разделов, не выполненные по существу

| Раздел | Пункт | Что не так |
|---|---|---|
| `years` | §20.1 «отличие от `/na-god`» | В статье 0 упоминаний «на год», «календарный», «персональный», `na-god`; смысловой ссылки нет ни туда, ни обратно. У `loops` симметричный пункт выполнен: тексты `/programmy` и статьи различны, взаимные ссылки есть |
| `past_lives` | §9 «развести интенты хаба и статьи раздела» | Хаб `/encyclopedia/karmic-tail` и `/encyclopedia/position/past_lives` бьют в один запрос «кармический хвост в матрице судьбы» (первым в `queries` у обоих), обе индексируются |
| `purpose` | §10.4 «итог: какой уровень уже проживается, где разрыв» | Итог ничего не вычисляет, а просит читателя сделать это самому: «Отметьте, какой из четырёх уровней…» |
| 11 разделов из 20 | «пример» в первом пункте каждого раздела | Разобранного примера нет у `past_lives`, `purpose`, `money`, `money40`, `relations`, `parents_children`, `ancestry`, `body_resource`, `chakras`, `rest`, `loops` (замерено: ни одной пары чисел вида `N–M` и ни одной главы со словом «Пример»). На её месте служебный блок `Пример полного персонального разбора` (`page.tsx:186-200`), абзац которого байт-в-байт одинаков на 17 страницах |
| `relations` | §13.1 | Панель «Как читать позицию» пишет «K внутрь формулы не входит», хотя K — четвёртая роль раздела и ей посвящена глава 5 той же статьи |
| `realisation` | §4.3 «связь `D↔социальное` только там, где она добавляет новый смысл» | Условия нет: ребро рендерится на 72 из 86 страниц — везде, где арканы различаются |
| `purpose` | §10.3 порядок графа | Лид обещает «личное→социальное, затем оба соединяются в духовном, а духовный переводится в планетарный», а рендер выдаёт синтез последним (`sectionReadings.ts:504`) |

- [x] Дописать в `years` разграничение с `/na-god` и поставить взаимные ссылки. **Сделано**: в
      первую главу статьи добавлен абзац «Возрастная шкала — не то же самое, что персональный год…
      сначала фон десятилетия, потом год внутри него», а на `/na-god` — блок «Фон десятилетия под
      расчётом года» со ссылкой на статью. Перелинковка стала взаимной.
- [x] Развести целевые запросы хаба хвостов и статьи раздела `past_lives`. **Сделано**: запрос
      «кармический хвост в матрице судьбы» оставлен хабу — он каталог формулы. Статья раздела
      переведена на свой интент: `seo.title` «Задачи прошлых воплощений в матрице судьбы — как
      читать хвост», пять запросов про прошлые воплощения и прошлые жизни. Пересечений между
      списками больше нет.
- [x] Дать `purpose` вычисляемый итог. **Сделано**: из четырёх арканов выводимы ровно два
      предметных факта — повтор аркана на двух уровнях («тема уже несётся через масштаб») и
      соседняя пара вне этой темы («разрыв»). Первая версия правила называла разрывом первую пару
      с разными арканами и давала **один и тот же текст на 37 результатах из 42**; переделал так,
      что разрыв ищется только относительно повторяющейся темы. Итог: 38 уникальных «напряжений»
      и 22 уникальных «силы» из 42 результатов.
- [x] Добавить главу-пример в 11 разделов, где её нет. **Сделано, и оказалось 12**: `years` тоже
      был без разобранного примера. Все примеры написаны на контрольной матрице 4–3–22 и сверены
      с движком: `purpose` 22–8–3–11, `money` 8–13–5–8, `relations` 15–20–5–7, `ancestry`
      15–13–22–11, `years` 4–7–3–7–22–6–11–15 и так далее. Из 20 разделов **17 примеров теперь
      сверяются автоматически** сторожем `check_examples`; три (`karma40`, `family_gifts`,
      `chakras`) — нет, у них в заголовке нет полной последовательности: у чакр она из десяти
      чисел и в заголовок не помещается.

      Побочно всплыло два расхождения между сторожами, оба устранены: `content.test.ts` требовал
      **ровно** 8 глав, а `content/validate.py` — не меньше 8; привёл vitest к тому же правилу
      (уникальность заголовков теперь проверяется по фактическому числу глав). И мой же валидатор
      поймал одинаковые зачины у трёх новых примеров — переписал.
- [x] Исправить `reading` у `relations` в `content/data/sections.json`. **Сделано**: было «K…
      внутрь формулы не входит» при том, что K — четвёртая роль раздела. Стало «M–R1–R как вход,
      главный узел и форма союза, а K — внутренний ресурс… Все четыре роли входят в разбор».
- [x] Ввести условие для `D↔социальное`. **Сделано**: у рёбер появилось необязательное условие
      `when`, и эта связь печатается только когда все три аркана различны — иначе цепочка
      D→личное→социальное уже содержит ту же проверку. Было 72 страницы из 86, стало 68.
- [x] Согласовать порядок связей `purpose` с тем, что обещает лид. **Сделано**: синтез встаёт
      сразу после последней связи, которая касается его источников. У `purpose` порядок стал
      «личное–социальное → синтез → духовное–планетарное», как и обещает лид; у `soul_tasks`,
      `ancestry` и `body_resource` порядок не изменился.

### 2.5. «22 общих текста для повторов `N+N`» не написаны

Требование §1, §2, §3, §5 итерации 3: «подключить 22 общих текста для повторов `N+N`». Корпуса нет:
`web/content/combinations.json` содержит ровно 231 пару (C(22,2)), пар вида `N-N` — ноль,
`/encyclopedia/combination/8-8` отдаёт 404. Блок повтора синтезируется одним шаблоном из
`plus[0..1]` / `minus[0..1]` (`web/lib/character.ts:148-165`, `web/lib/sectionReadings.ts:414-435`).
Выход различается по арканам, но это не 22 написанных текста.

- [x] Решить: писать 22 текста повтора или переформулировать требование. **Решение: написать. Все
      22 текста написаны и подключены.**

      Требование итерации 3 понято правильно: блок повтора собирался шаблоном из `plus[0..1]` и
      `minus[0..1]` («В сильном проявлении человек …, а при перекосе …»), то есть выход различался
      номерами, но текста не было. Теперь у каждого аркана есть своё поле `repeat` в
      `content/data/arcana.json` — 2–3 предложения, 299–354 знака: что даёт удвоение темы и чем оно
      обходится. Пример для Мага: «Удвоенный старт делает напор сквозным: там, где другие ещё
      выбирают, человек уже начал — и начал сразу в двух разных вопросах… Различайте, где нужен
      первый шаг, а где доведение, иначе набирается пачка ярких начал без единого финала.»

      Подключено в оба места, где печатался шаблон: `web/lib/character.ts` (повтор аркана в ролях
      характера, 724 результата из 5 544) и `web/lib/sectionReadings.ts` (повтор в ролях раздела).
      Первый абзац блока по-прежнему собирается движком — в нём подставляются конкретные позиции.

      Проверки: корпус — 22 из 22, длина в норме, зачины разные, `plus`/`minus` дословно не
      вставлены, дублей с позиционным корпусом нет; в сборке поле обязательно
      (`content/build.py`, `content/validate.py` — минимум 200 знаков) и в типе фронта
      (`web/lib/content.ts` отвергает аркан без `repeat`). Прогон: `content.validate` — 6 065
      проверок, 0 ошибок; `npx vitest run` — 362 из 362.

      **Отдельные страницы `N-N` в энциклопедии сочетаний не создаются** (`/encyclopedia/combination/8-8`
      остаётся 404): корпус сочетаний — это 231 пара разных арканов, а повтор одного аркана живёт
      внутри разбора, где известны конкретные позиции. Ссылок на `N-N` в продукте нет — проверено:
      ветка повтора не отдаёт `href`.

---

## 3. Регрессия текстов

Все тексты этого раздела живут в одном файле: `content/data/in-positions.json`, структура
`{"<аркан 1..22>": {"<ключ раздела>": "текст"}}`. Оттуда они собираются в `web/content/arcana.json`
(`tools/seo/build-content.py`), поэтому после правки нужна пересборка контента.

**Где взять «было».** Сравнения ниже сделаны против версии до итерации 3. Пока работа не
закоммичена — `git show HEAD:project/destiny-matrix/content/data/in-positions.json` против
`git show :project/destiny-matrix/content/data/in-positions.json`. После коммита `HEAD` уедет, и
опорой остаётся **коммит `084b2ff`**:

```bash
git show 084b2ff:project/destiny-matrix/content/data/in-positions.json > /tmp/before.json
git show HEAD:project/destiny-matrix/content/data/in-positions.json    > /tmp/after.json
```

Изменено 235 толкований арканов. Разобрано каждое.

| Раздел | Хуже | Так же | Лучше | Средняя длина было→стало |
|---|---|---|---|---|
| `family_gifts` | 22 | 0 | 0 | 169 → 400 |
| `resources` | 22 | 0 | 0 | 175 → 325 |
| `money` | 21 | 1 | 0 | 181 → 336 |
| `ancestry` | 21 | 1 | 0 | 164 → 331 |
| `years` | 20 | 2 | 0 | 167 → 401 |
| `body_resource` | 18 | 4 | 0 | 178 → 394 |
| `relations` | 8 | 11 | 3 | 180 → 301 |
| `rest` | 8 | 9 | 5 | 175 → 308 |
| `profession` | 7 | 10 | 5 | 180 → 352 |
| `realisation` / `parents_children` / `purpose` / `money40` / `karma40` | 33 | 3 | 1 | — |

### 3.1. Механизмы ухудшения

- **Конкретика заменена абстракцией.** «спит по пять часов и не устаёт» → «день пролетает на одном
  дыхании»; «в голодный год кормила пятерых и никого не потеряла» → «в трудный год кормил большую
  семью»; «получается втрое больше» → «заметно больше»; «прошёл войну» → «прошёл тяжёлый переезд».
- **Хеджирование съело утверждение.** «может/могут» по файлу 74 → 162. «Стабильный оклад здесь
  занижает доход» → «может не использовать готовность работать с возможностями».
- **Пришитый императив.** 86 из 110 текстов пяти разделов кончаются заданием, часто повторяющим
  тело. `resources/19`: «Достаточно рассказывать о своей работе там, где её видят люди» и сразу
  «Расскажите о своей работе там, где её увидят нужные люди».
- **Повторы внутри абзаца.** `family_gifts/1` — «смелость начать без готовой опоры» трижды в
  четырёх предложениях; `ancestry/13` — «известн-» четыре раза.
- **Один каркас на 22 аркана.** `years`: 22 из 22 — «Десятилетие ‹аркан› похоже на… В сильном
  прохождении… риск —… К следующему этапу…». `body_resource`: 22 из 22, причём в 6 текстах не
  осталось ни одного телесного слова — раздел про ресурс тела стал разделом про приоритизацию задач.
- **Потеря доменной лексики.** В `money` плотность денежных слов упала вдвое при двукратном росте
  объёма; метафора «канал» — 17 употреблений → 1.

### 3.2. Ошибки, внесённые правками

| Где | Что |
|---|---|
| `money40`, аркан 18 | «может **опереться на того**, что раньше казалось странностью» — грамматика; уже в собранном `web/content/arcana.json` |
| `realisation`, аркан 18 | «его **особый** взгляд **особенно** полезен» |
| `money`, арканы 2 и 8 | два тире подряд в одном предложении — след механической замены «Канал перекрывается X — …» на «Слабое место — X — …» |
| `family_gifts`, аркан 11 | текст начинается с «это» без антецедента: подлежащее было в удалённом зачине |
| `realisation` | слово «положено» снято в 12 текстах и оставлено в аркане 14 |
| `parents_children` | «сын» заменён на «ребёнок» в аркане 3 и оставлен в аркане 6 |
| `money40` | оборот «Практическим вариантом может быть» ×3 — новый шаблон вместо снятого «Хорошее время…» |

### 3.3. Заголовки статей

| Файл | Было | Стало | Проблема |
|---|---|---|---|
| `comfort` | «Что даёт вам внутренний комфорт» | «Центр и внутренние точки» | читательский заголовок заменён внутренним жаргоном, при том что все 5 `queries` — про «зону комфорта» |
| `chakras` | «Карта энергий **по чакрам**» | «Карта энергий: толкование семи уровней» | из H1 выброшено слово «чакры» — запрос №1 в списке |

### 3.4. `body_resource`: где стерилизация оправдана, а где нет

**Оправданно убрано** (медицинское утверждение, режимное предписание, БАДы): «Режим сна работает
лучше любых добавок», «Кофе и ночная работа дороже всего обходятся», «никакие витамины этого не
меняют», «Ресурс приходит через еду, сон, воду», «тело изнашивается».

**Обеднело зря** (ни диагноза, ни рецепта): «Долгая ссора выбивает сильнее физической нагрузки»,
«Ломается не от труда, а от невозможности его выплеснуть», «Первый признак истощения — раздражение
от людей, которых обычно терпит легко», «истощают уродство, теснота и бессмысленная рутина»,
«дорога восстанавливает лучше отпуска на диване».

Числа: `сон/спать/недосып` 13 арканов → 0; `тело/телесн` 11 → 0; `истощ*` 14 → 0; `восстанавлив*`
15 → 0.

### 3.5. Общие статьи: оборонительный регистр и непредметный FAQ

Замерено по 20 статьям:

| Метрика | Значение |
|---|---|
| Предложений в главах | 600 |
| Из них с отрицанием («не», «ни», «нет», «без») | **227 (38 %)** |
| Глав, где есть хотя бы одно отрицание | **146 из 160 (91 %)** |
| Вопросов FAQ | 100 |
| Из них в четырёх сквозных слотах (повтор аркана · дисклеймер · «как проверить» · порядок чтения) | **53** |
| Ответов, начинающихся со слова «Нет.» | **35** |
| Объявленных в `seo.queries` фраз | 104 |
| Из них дословно встречаются в вопросах FAQ | **2** |
| Вопросов «как рассчитать/посчитать» | 1 на все 20 разделов |
| Вопросов со словами «по дате рождения» | 0 |

То есть статья по регистру — это «что метод НЕ делает», а FAQ отвечает не человеку из поиска, а
редактору, который боится претензий. Плюс разрыв голоса: новый оборонительный текст стоит прямо под
старым `meaning`, написанным в другой интонации. На `/encyclopedia/position/rest` подряд идут:

> «Отдых — раздел, который чаще всего игнорируют, а потом удивляются выгоранию… возвращается в
> понедельник уставшим сильнее, чем уходил.»
> «Раздел не выдаёт универсальный список занятий и не назначает единственно правильный отпуск.»

- [x] Снизить долю отрицаний. **Сделано и замерено** по 19 статьям с главами:

      | Метрика | было | стало |
      |---|---|---|
      | предложений в главах | 602 | **915** |
      | из них с отрицанием | 240 (**40 %**) | 95 (**10 %**) |
      | глав, где есть отрицание | 151 из 164 | **86 из 164** |
      | ответов FAQ, начинающихся с «Нет» | 34 | **10** |
      | слов в главе | 45–69 | **70–123** |
      | слов в статье | ~450 | **744** |

      Правило замены соблюдено: дисклеймер оставлен там, где без него текст обещает («сумму, дату
      выплаты и источник дохода линия не рассчитывает», «не заключение о состоянии организма»,
      «не совместимость двух дат»), а в остальных местах вместо «раздел не выдаёт список занятий»
      написано, что раздел выдаёт.
- [x] Переписать FAQ под реальные поисковые интенты. **Сделано**: дословных фраз из `seo.queries` в
      вопросах было **2 из 137**, стало **70 из 98** (счёт по 19 файлам со статьями). Вместо
      четырёх сквозных слотов пошли вопросы поиска: «как рассчитать аркан зоны комфорта в матрице
      судьбы», «что означает аркан детей в матрице судьбы», «где находится линия таланта».

      Ограничение, найденное прогоном: две фразы `money` — «денежный канал в матрице судьбы» и
      «денежный канал матрица судьбы» — в вопрос поставить нельзя, «денежный канал» стоит в списке
      буквальных прогнозов (`web/lib/content.test.ts:121`). Взяты остальные три запроса раздела.
- [x] Согласовать интонацию новых глав со старым `meaning`. **Сделано**: `meaning` не менялся
      (сверено побайтно с копией до правок), под него подстроены главы — короткая фраза, конкретный
      образ, без «важно понимать». Расширение шло только предметным: формулы точек из
      `spec/method.json`, числовые примеры от контрольной матрицы 4–3–22 и детали арканов из
      `in-positions.json`.

**Чем это проверено, кроме метрик.** Работу вели пять редакторов по своим файлам, затем два
независимых ревизора сверяли результат с копией «до» и с движком. Вердикты: «вернуть» и «принять с
оговоркой» — оба с проверяемыми списками, а не впечатлениями. Найдено и исправлено **12 дефектов**:

- `profession` потерял обязательную оговорку «доход зависит от навыков, спроса, качества результата
  и условий работы» вместе с удалённым вопросом FAQ — на неё стоит именной сторож;
- `purpose`, `realisation`, `soul_tasks` обещали читателю поведение отчёта, которого нет: «вторая
  карта с тем же числом отсылает к первой». Механизм `sameAs` работает только у ролей с
  `pairedWith`, а он задан ровно в четырёх разделах — `family_gifts`, `ancestry`, `relations`,
  `money`;
- `purpose` утверждал «формулы уровней независимы, поэтому одно и то же число часто встаёт на две
  позиции». Проверено перебором 22 280 дат: уровни считаются друг из друга (духовный = личное +
  социальное, планетарный = социальное + духовное), повтор — **12,8 %**, а не «часто»;
- `soul_tasks` выдумал геометрию карты («земные точки внизу отвечают за деньги и отношения»). По
  `content/data/points.json` линия земли — это арканы дня и года, а деньги идут по L–R2–R;
- `family_gifts` читал биографию рода прямо с аркана («в роду теряли дом», «в семье был человек,
  который смехом вытаскивал остальных») и потерял прямой отказ от такого чтения — вернул вопросом
  «Точно ли матрица показывает, что происходило в роду?»;
- `resources` потерял отказ от обещания достатка, `money40` предсказывал события в изъявительном
  наклонении, `loops` противоречил себе (§4 «полного освобождения не обещает» против FAQ «сюжет
  закрывается за два-три раза») и имел нечитаемую склейку;
- плюс пять коллизий, которые ловит штатный валидатор: одинаковые зачины и дубли между файлами,
  а также `h2` «Пример круга 11–4–3 **в матрице 4–3–22**» — `check_examples` разбирает любую
  числовую последовательность в заголовке и сверяет её с движком.

После правок: `content.validate` — **6 043 проверки, 0 ошибок**; `npx vitest run` — 362 из 362;
`pytest engine content` — 63 из 63.

### 3.6. `profession.meaning` переписан хуже оригинала

Единственный `meaning`, переписанный целиком, — и это витрина раздела:

| Было | Стало |
|---|---|
| «отвечает **не** на вопрос „кем работать“, **а** на вопрос…» | «отвечает **не столько**… **сколько**…» — антитеза размыта |
| «результат, за который **платят**» | «за который **готовы платить**» |
| «форма труда, в которой человек силён от природы и **в которой он не выгорает**» | «силён и **способен расти без постоянной войны с собой**» |
| «говорит **не о том**, что вам приятно…, **а о том, что мир готов у вас купить**» | «говорит **не только** о том…, **но и** о том, что **может оказаться достаточно полезным, чтобы** мир захотел это купить» |
| «Практический вывод **один**» | «Практический вывод **остаётся живым и земным**» |

Вырезано: «самая частая история раздела: способности есть, а дохода и радости от работы нет»,
«труд наполняет, а деньги приходят как естественное следствие», «доход упирается в потолок».

- [x] Вернуть антитезы и вырезанные формулировки; убрать «Практический вывод остаётся живым и
      земным». **Сделано**: восстановлены все пять антитез («не на вопрос „кем работать", а на
      вопрос…», «не о том, что вам приятно…, а о том, что мир готов у вас купить», «за который
      платят», «в которой он не выгорает», «Практический вывод один») и вырезанные образы
      («самая частая история раздела: способности есть, а дохода и радости от работы нет»,
      «доход упирается в потолок»).

      **Две «потери» восстанавливать нельзя, и ревью это не учло** — на них стоит именной сторож
      `web/lib/content.test.ts:97-99`: «деньги приходят как естественное следствие» (буквальное
      обещание дохода) и «дело не в человеке и не в усилиях» (снимает с читателя ответственность).
      Первую заменил на «труд наполняет, а доход становится следствием сделанного», вторую — на
      «стоит проверять не сферу целиком, а задачи, рабочую среду и способ, которым результат
      встречается с людьми». Обязательная оговорка «доход зависит от навыков, спроса, качества
      результата и условий работы» осталась на месте (в главе, а не в `meaning`).

      Прогон: `content.test.ts` 13 тестов зелёные, `content.validate` 5 970 проверок / 0 ошибок.

### Чеклист по текстам

- [x] Вернуть конкретные образы там, где они были вырезаны без причин безопасности. **Сделано**
      силами пяти редакторов с независимой сверкой каждой группы: предложено 217 правок, применено
      **210**, семь отклонены рецензентами за безопасность (`money40`/7 — обещание отдачи без
      хеджа, `ancestry`/13 — ближайшее к «родовому проклятию», `body_resource` 1/14/15/18 — возврат
      вырезанного про сон, голод и ночную работу, `years`/19 — снятый хедж со списка, где стоят
      дети). Вернулись «голодный год, кормила пятерых и никого не потеряла», «прошёл войну»,
      женщина, которая «знала про всех всё и молчала», антитеза «через людей, а не через силу —
      там, где другие ломились».
- [x] Снять хеджирование там, где оно не защищает от обещаний. **Сделано и замерено** по 308
      текстам 14 разделов:

      | Метрика | до итерации 3 | итерация 3 | сейчас |
      |---|---|---|---|
      | «может / могут / возможно» | 33 | **155** | **43** |
      | «стоит / полезно / проверить» | 30 | **123** | **65** |
      | шаблонные обороты итерации 3 | 0 | 6 | **0** |
      | императивные довески | 0 | **70** | **30** |
      | средняя длина текста, знаков | 179 | 293 | 257 |

      Из 308 текстов 94 вернулись к формулировке до итерации 3, 97 остались как были (правка была
      оправданной или защитной), 117 получили новую редакцию.

      **Важное ограничение, найденное прогоном:** редакторы вернули 19 формул, на которые в
      репозитории стоит явный сторож (`web/lib/content.test.ts`): «деньги приходят», «денежный
      канал», «канал открывается/закрывается», «этому человеку положено», «почти наверняка»,
      «партнёру придётся», «долгие отношения держатся». Их убрали в итерации 3 **осознанно**, и
      ревью зря записало это в потери. Все 19 переписаны без обещающих формул, конкретность и ритм
      сохранены.
- [x] Убрать императивные довески, дублирующие тело текста. **Сделано**: 70 → 30, и оставшиеся
      несут проверяемый шаг, а не пересказ уже сказанного.
- [x] Исправить 7 внесённых ошибок из §3.2. **Сделано**: `money40`/18 «может опереться на того» →
      «опирается на то»; `realisation`/18 «его особый взгляд особенно полезен» → «именно там его
      взгляд и оказывается нужен»; `money`/2 и /8 двойное тире → двоеточие; `family_gifts`/11
      «это мог быть тот» без антецедента → «есть тот»; `realisation`/14 «положено» → «важно»
      (в файле не осталось ни одного «положено»); `parents_children`/6 «из сына» → «из ребёнка».
- [x] ~~Вернуть ключевые слова в `title` `comfort` и `chakras`~~ — **пункт снят проверкой**.
      Ключевые слова живут в `seo.title`, и он не менялся: «Зона комфорта в матрице судьбы — что
      вас наполняет», «Чакры в матрице судьбы — карта энергий по дате рождения». Поле `title` —
      это имя раздела в навигации и крошках, и оно заперто на `engine.sections`: валидатор
      требует совпадения (`заголовок/вводка/доступ разъехались с engine.sections`). Ревью сравнило
      не то поле.
- [x] Сломать одинаковый каркас у `years` и `body_resource`. **Сделано во всех 44 текстах, а не в
      половине.** Каждый доведён до ровно четырёх предложений — суть / сила / риск / действие, —
      поэтому движку больше не нужны служебные маркеры в прозе:

      | Оборот | `years` до | `years` после | `body_resource` до | после |
      |---|---|---|---|---|
      | «В сильном прохождении» / «Сильное прохождение» | 19 из 22 | **0** | 22 из 22 | **0** |
      | «; в риске» / «риск —» | 21 из 22 | **0** | 15 из 22 | **0** |
      | «К следующему этапу» | 21 из 22 | **0** | — | — |
      | зачины совпадают по двум первым словам | — | не более 2 при пределе 3 | — | максимум 1 |

      Сила и риск теперь берутся из собственного текста у **22 из 22** в обоих разделах (до правки
      `years` 4 из 22, `body_resource` 0 из 22 — редактура §3.1 сняла маркеры, на которых держался
      разбор §1.3); все 44 формулировки силы и 44 формулировки риска различны. Ни одно предложение
      корпуса не теряется: ветка `parts.length >= 4` отдаёт середину кубиками, а не выбрасывает её.

      Проверено прогоном на пропатченном корпусе до применения: запрещённые формулы прогноза и
      медицинская регулярка — 0 совпадений, канцелярит — 0, дублей текстов и первых 30 знаков нет,
      уникальность предложений по всем 440 текстам разделов держится, длина 371–594 знака при
      пороге 140. `content.validate` после сборки: 5 970 проверок, 0 ошибок.

      **Оговорка о том, куда попадает результат.** Раздел `years` читает позицию через вариантную
      рамку `decade`, поэтому написанные сила и риск идут в **суть** роли, а кубики силы и риска
      берут канонические plus/minus аркана (см. §2.2). В кубики они попадают там, где позиция
      читается без рамки: статьи сочетаний и `/encyclopedia/position/years`.
- [x] Довести до конца незавершённые правки («положено», «сын»). **Сделано** вместе с §3.2:
      проверено грепом — в корпусе не осталось ни «положено», ни «из сына товарища».

---

## 4. Дыры в проверках

### 4.1. Сторожа текста, которые не работают

| Сторож | Где | Что пропускает |
|---|---|---|
| канцелярит | `content/data/text-policy.json`, `style_patterns[0]` = `\bявляет(ся\|ются)\b` | **Регексп сломан**: основа «явля-», а не «являет-», поэтому «являются» не ловится. Проверено запуском: «является» → True, «являются» → False. Мимо идут 3 вхождения в FAQ статей `soul_tasks` и `chakras` |
| медицинская лексика | `content/data/text-policy.json`, группа `medical` — 19 префиксов + 3 фразы | Проходят «позвоночник и надпочечники», «наладить режим сна и пить витамины», «состояние щитовидной железы и гормональный фон», «профильный специалист» |
| шаблонные вводные (правило 7) | `web/lib/content.test.ts:17-34` — чёрный список из 8 первых слов; `content/validate.py:304-324` — совпадение первых 30 знаков | Мутация «Раздел «{название}» в матрице судьбы читается по своим ролям…» в трёх разделах прошла обе проверки |
| обвиняющие семью формулировки | — | Сторожа нет вовсе |

- [x] Починить регексп канцелярита и перепроверить весь собранный корпус.

**Сделано.** Уточнение к диагнозу: `\b` тут ни при чём — в Python он работает с кириллицей
(остальные 15 шаблонов срабатывают). Ошибка была только в основе слова: `являет(ся|ются)` даёт
«является» и несуществующее «являетются». Исправлено на `явля(ется|ются)` в
`content/data/text-policy.json`. TS-сторона не затронута: `style_patterns` там только объявлены в
типе и не применяются, а свой матчер `web/lib/textPolicy.ts` строит регулярки с явными классами
букв и флагом `u`.

Красный → зелёный получился сам собой: с починенным шаблоном валидатор нашёл ровно три вхождения —
`soul_tasks` FAQ 1 и `chakras` FAQ 1 и 2. Все три переписаны без канцелярита в
`tools/seo/content/positions/*.json`, контент пересобран, `content.validate` — 5922 проверки,
0 ошибок.
- [x] Расширить медицинский список: органы, симптомы, режим, добавки, «специалист».

**Сделано, но не буквально: пункт про «специалиста» отменён по итогам проверки.** Список вырос
с 19 префиксов и 3 фраз до **34 и 7**: добавлены органы (`позвоночник`, `надпочечник`, `щитовидн`,
`желудок`, `кишечник`), режим и добавки (`витамин`, `бад`, `голодани`, `детокс`, `диет`, `сахар`),
практики (`остеопат`, `гомеопат`, `нутрициолог`) и фразы `режим сна`, `обмен веществ`,
`гормональный фон`, `уровень гормонов`.

Проверка на живом корпусе после каждого шага отсеяла три кандидата, которые дали бы ложные
срабатывания:

| Кандидат | Почему убран |
|---|---|
| `давлени` | 12 попаданий, и все переносные: «под давлением обстоятельств», «публичное давление» |
| `самочувстви` | стоит внутри собственного дисклеймера «не делая выводов о самочувствии» |
| `гормон` как префикс | ловил статью чакр: «не измерение органов, **гормонов** или состояния организма» — то есть отрицание. Заменён на фразы-утверждения `гормональный фон` и `уровень гормонов` |

**«Специалист» блокировать нельзя, и это вывод по существу.** Фразы вида «обратитесь к профильному
специалисту» поймали `body_resource` FAQ 5: «Что делать при боли, бессоннице или стойком
дискомфорте? — Обратиться к профильному специалисту». Это не медицинский совет, а отказ его давать
и направление к врачу — ровно то, что продукт обязан говорить. Сторож из подстрок не отличает совет
(«пейте витамины») от отказа советовать, и блокировка второго выдавила бы из текста нужное
предупреждение. Фразы убраны.

**Настоящие попадания нового списка исправлены.** Гард нашёл два совета про режим сна в сочетаниях:
`2-18` — «**Режим сна**, вода и правило не решать в тёмной фазе спасают почти всё» → «Тишина, вода
и правило…»; `17-18` — «счёт на неделю, **режим сна** и один человек» → «счёт на неделю, ровный
распорядок и один человек». Правки внесены в исходник `content/data/pairs.json`, корпус пересобран.

Итог: `content.validate` — 5922 проверки, 0 ошибок; `content.selftest` — «валидатор ловит все
случаи». Из шести фраз, которые раньше проходили насквозь, теперь ловятся пять; шестая — тот самый
отказ советовать, который ловить не нужно.
- [x] Заменить чёрный список шаблонных вводных на проверку по существу (например, схожесть первых
  предложений между разделами).

**Сделано.** В `content/validate.py` добавлен `check_section_openings`: первая фраза каждой главы
берётся с **замаскированным названием раздела** и сравнивается со всеми остальными по первым восьми
словам. Подстановка названия перестаёт делать текст «уникальным» — а именно на ней прежние сторожа
и ломались.

Проверено: на текущем корпусе 160 глав и **0 совпадений** — ложных срабатываний нет. Мутация из
§4.1 (один и тот же абзац «Раздел «‹название›» в матрице судьбы читается по своим ролям и связям…»,
разложенный по `rest`, `loops`, `money40`) теперь ловится: две ошибки вида «шаблонная вводная:
„rest · глава 1“ повторяет „money40 · глава 1“». Чёрный список из восьми слов в
`web/lib/content.test.ts` оставлен: он дешёвый и стережёт свой конкретный прошлый дефект в быстром
цикле.

**Закрыт и разрыв, из-за которого этих сторожей не было видно.** `content/validate.py` запускался
только из `compose/scripts/run-tests.sh`, поэтому «зелёный pytest» ничего не говорил о контенте.
Добавлен `content/tests/test_artifacts.py::test_content_validator_runs_inside_pytest` — валидатор
теперь идёт и в быстром цикле (`pytest content` — 17 тестов, зелёные).

### 4.2. Правило 6: что не покрыто автотестами

| Проверка | Покрыто | Не покрыто |
|---|---|---|
| `noindex` персональной статьи | 19 из 19 | — |
| адаптивность | 20 разделов на 390 и 1366 px | **320 и 768 px — нигде** (там и живёт дефект 1.5) |
| self-canonical персональной статьи | **0 разделов** | `web/lib/site.ts::pageMeta` не покрыт ни одним юнитом |
| HTTP-404 маршрута | 4 раздела | остальные 16 |
| путь из калькулятора кликом | 1 (`comfort`) | остальные 19 |
| совпадение отчёта и PDF | по сути 0 | 20 разделов |
| формы повторов | comfort, profession, один кейс `years` | остальные 15 |
| наличие 20 общих статей в sitemap | **0** — `web/app/sitemap.test.ts` проверяет только отсутствие персональных | — |
| запрещённая лексика на всех результатах | 3 слага на раздел (первый, средний, последний), `sectionReadings.test.ts:168` | остальные ~25 тыс. слагов; пункт «проверить N результатов» на медбезопасность выполняется выборочно |
| числовые примеры в статьях | **0** — `content/validate.py` не сверяет их с движком | все 20 разделов (дефект 1.4 так и прошёл) |

- [x] Добавить 320 и 768 px в браузерную проверку переполнения.
- [x] Покрыть `pageMeta` юнитом: `noindex`, `follow`, self-canonical, уникальность метаданных.
- [x] В `sitemap.test.ts` добавить утверждение «все 20 общих статей присутствуют» — сейчас потеря
  раздела оставит тест зелёным.
- [x] Сравнивать экранный отчёт и PDF автоматически, а не искать в PDF 10 фиксированных строк.

**Сделано.**

| Дыра | Чем закрыта |
|---|---|
| 320 и 768 px нигде | `test_all_20_personal_article_layouts_fit_mobile_and_desktop` — пять ширин × 20 маршрутов (см. §1.5) |
| `pageMeta` без единого юнита | новый `web/lib/site.test.ts`, 5 тестов: абсолютный self-canonical; `noindex` с `follow` и без; `og:type` совпадает с разметкой `Article`; длинный заголовок не режется суффиксом бренда; по три слага каждого из 18 разделов — уникальные заголовок и canonical, `robots: {index:false, follow:true}` |
| sitemap не утверждал наличие статей | `app/sitemap.test.ts`: `SPEC` должен содержать 20 разделов, и для каждого в карте есть `/encyclopedia/position/<key>`. Проверено на живом дефекте — при выпадении `rest` из карты тест краснеет: «разделов нет в карте сайта: expected [ 'rest' ]» |
| состав PDF по десяти строкам | в `test_method_release.py` добавлена сверка со `spec/sections.json`: в файле должны быть все 20 заголовков (см. §1.7) |

Прогон: web unit **356 из 357** (единственное падение — календарный паритет), typecheck ✓,
`pytest engine content` 62 из 63 (то же падение).

### 4.3. Экранный отчёт и PDF не совпадают по объёму

`web/components/matrix/ReportSections.tsx:111-118`: при `printing` рендерится полный
`CharacterReadingView`, иначе только `CharacterConclusionView`. Итог (`summary`, `strength`,
`tension`, `practice`) совпадает дословно, но PDF является надмножеством экрана: маркеры «Рамка
0–10 лет», «Переход 10 лет», «Возвращение темы», «Резкая смена темы», «От корня к автопилоту» есть
в PDF и отсутствуют в отчёте. Обратно: «Как складывается тройка» — 19 раз в отчёте, 0 в PDF.

Формулировке правила 6 «совпадение отчёта и PDF» это соответствует не буквально. Возможно, решение
осознанное — тогда его надо записать, потому что сейчас оно нигде не описано и ничем не проверяется.

- [x] Зафиксировать решение: PDF богаче экрана намеренно или это расхождение.
- [x] Что бы ни решили — закрыть проверкой, сверяющей состав, а не отдельные строки.

**Решение: это осознанно и правильно, менять нечего.** Вся архитектура итерации 3 построена на том,
что экран отдаёт итог и уводит двумя штатными ссылками в `/encyclopedia/`, где лежит полный разбор.
В PDF ссылки мертвы — значит он обязан быть самодостаточным. Разница в объёме (143 463 знака против
65 396) — следствие этой развилки, а не потеря.

Инвариант, который действительно должен держаться, — **совпадение итога**, и он держится: четыре
части (`summary`, `strength`, `tension`, `practice`) совпадают в отчёте, PDF и энциклопедии
дословно, сверено.

- [x] При пересмотре плана итерации 3 поправить формулировку правила 6. **Сделано**:
      `seo-plan-iter-3.md` правило 6 теперь требует «совпадение **итога** отчёта, PDF и
      персональной статьи» и прямо называет четыре части итога плюс причину, по которой PDF богаче
      экрана. Формулировки «равенство отчёта и PDF» в чеклистах разделов оставлены как есть: они
      уже закрыты и теперь читаются через уточнённое правило.

Состав закрыт проверкой (см. §1.7): в PDF должны присутствовать все 20 заголовков из
`spec/sections.json`, а не десять фиксированных строк.

---

## 5. Мелочи

- [x] ~~`repeatedSummary` продублирована байт-в-байт~~ — **сделано**: копия из
      `sectionReadingShared.ts` убрана, функция экспортируется из `comfortReading.ts` и
      импортируется оттуда. Обратный импорт невозможен (цикл), поэтому источником осталась
      та сторона, от которой уже зависит вторая.
- [x] ~~Название раздела разъехалось~~ — **сделано**: в разборе поставлено каноническое «Что
      открывает вам блага и ресурс» из `spec/sections.json`. Сверил все 18 разделов — расхождение
      было единственным.
- [x] ~~`CharacterConclusionView` подписывает «Как складывается тройка» у четырёхрольных~~ —
      **сделано**: подпись зависит от числа ролей — «пара», «тройка», «роли раздела».
- [x] ~~`.character-roles` — жёсткие 3 колонки, ряд 3+1~~ — **сделано**: колонки подбираются
      `auto-fit`, а четыре карточки ложатся 2×2.
- [x] ~~`/encyclopedia/years/<slug>` без `?birth=` показывает чужую дату~~ — **сделано**: без
      параметра линия отдаётся без персонального возраста. Заголовок «…: линия 4-7-3-…», восемь
      периодов на месте, текущий и следующий этап не отмечаются, итог зовёт открыть раздел из
      своего расчёта. Закрыто юнитом в `sectionReadings.test.ts`.
- [x] ~~Статья `chakras` не ссылается на семь дочерних~~ — **сделано**: поле `links` не доходило
      до страницы — его не было в типе `PositionContent`. Теперь оно читается и выводится блоком
      «Уровни карты по отдельности».
- [x] ~~Страница хвоста не ссылается на статью раздела~~ — **сделано**: в блок «Куда дальше»
      добавлена ссылка «Как читается раздел „Задачи прошлых воплощений“».
- [x] `money40` и `resources` делят 78 одинаковых строк-слагов. **Решение: порядок оставлен,
      менять URL не за что.** Порядок ролей в слаге повторяет порядок из `spec/sections.json`: у
      `resources` там сначала L, у `money40` — сначала R2 («Как перечитать **R2 и L** через
      накопленный опыт» — это лид раздела). Переставить роли значит разойтись со спецификацией и
      лидом; сортировать слаг отдельно от порядка ролей — значит разойтись с заголовком страницы,
      который печатает арканы в порядке ролей («…13-8: Смерть, Сила»). На самой странице
      неоднозначности нет: и слаг, и заголовок, и карточки ролей идут в одном порядке. Совпадение
      строк заметно только при сравнении двух адресов разных разделов, обе страницы `noindex`.
- [x] `/encyclopedia/position/day` — 7 глав вместо 8. **Сделано**: написана восьмая глава «Почему
      визитка расходится с самоощущением» — единственный угол, которого в статье не было: точка A
      отвечает за первое впечатление, а внутреннюю картину задаёт B, и разбор этой пары уже есть в
      энциклопедии сочетаний. 120 слов, как у соседних глав. Исключение из проверок снято:
      `content/validate.py` теперь требует 8 глав от **любой** страницы точки со статьёй, а не
      делает скидку именно `day`; `content.test.ts` поднят с 7 до 8.
- [x] **`NoFallbackError` на маршрутах энциклопедии. Причина найдена и снята.** Прошлый диагноз
      («починка в поведении Next под layout, заслуживает отдельного захода») **неверен**: layout ни
      при чём. Замер на dev-сервере Next 15.5.23 показал, что `/na-god/nope` живёт под точно таким
      же `EncShell`-layout и отдаёт штатный 404.

      **Настоящая причина — общий маршрут `app/encyclopedia/[section]/[slug]`.** Он совпадает с
      любым адресом вида `/encyclopedia/что-угодно/что-угодно`, поэтому `/encyclopedia/arcanum/99`
      не отваливался на маршрутизации (у своего маршрута стоит `dynamicParams = false`), а
      проваливался в перехватчик и получал `notFound()` уже на рендере. Runtime-`notFound()` Next
      отдаёт стримом: в HTML остаётся оболочка `__next_error__` с заголовком главной, а заголовок
      и текст 404 приезжают только RSC-пейлоадом.

      Решающий замер: убрал каталог `[section]` — те же три адреса сразу стали отдавать
      «Страница не найдена» с телом; вернул — снова оболочка.

      | Адрес | до | после |
      |---|---|---|
      | `/encyclopedia/arcanum/99` | `__next_error__`, заголовок главной | «Страница не найдена», тело есть |
      | `/encyclopedia/chakra/nope` | то же | то же |
      | `/encyclopedia/karmic-tail/1-2-3` | то же | то же |
      | `/encyclopedia/position/nope` | то же | то же |
      | `/encyclopedia/nosuchsection/1-2` | то же | то же |
      | `/encyclopedia/money/8-13-5-8` | 200, статья | 200, статья |
      | `/encyclopedia/years/4-7-…-15` | 200, статья | 200, статья |

      Что сделано: перехватчик удалён, его тело вынесено в `app/encyclopedia/_personal/reading.tsx`
      (каталог с подчёркиванием в маршрутизацию не попадает), и каждый из 16 разделов получил
      собственный путь `app/encyclopedia/<раздел>/[slug]/page.tsx` из шести строк.
      `comfort` и `profession` как раньше обслуживаются своими статическими маршрутами.
      Сторож — `web/lib/encyclopediaRoutes.test.ts`: маршрут обязан существовать для каждого ключа
      `PERSONAL_SECTION_KEYS`, а каталог `[section]` не должен вернуться.

      **Что осталось и почему.** Валидный раздел с недостижимым слагом
      (`/encyclopedia/money/1-1-1-1`) по-прежнему отдаёт 404 стримом: чтобы отсечь его на
      маршрутизации, нужен `dynamicParams = false` со списком всех достижимых слагов, то есть
      предрендер ~25 тысяч noindex-страниц — ровно то, от чего маршрут и уходил. Статус ответа при
      этом верный (404), страница `noindex`, а текст доезжает RSC-пейлоадом; проверенные тесты
      цикла этот класс не покрывают.
- [x] Объём общих статей. **Решение: не норма, поднято.** Глава была 45–69 слов при одном абзаце —
      стало **70–123 слова** в двух абзацах, статья вместо ~450 слов держит **744**. Расширение
      предметное: формулы точек, числовой пример от контрольной матрицы, что человек видит в отчёте
      и какой шаг из этого следует.
- [x] Служебный блок «Пример полного персонального разбора» одинаков на 17 страницах. **Сделано**:
      подпись собирается из ролей самого раздела и арканов контрольной матрицы —
      `sectionExampleNote` в `web/lib/sectionReadings.ts`. Было на 17 страницах одно предложение
      «применяет те же правила к одному достижимому результату», стало, например,
      «На результате 8–13 те же правила читают роли раздела: вход ресурса — Сила, условие
      удержания ресурса — Смерть». У `chakras` и `years` своя формулировка: у первой слаг из
      24 чисел в тексте не нужен, у второй роли — это восемь десятилетий.
      Проверка `sectionReadings.test.ts` требует, чтобы все 18 подписей были различны и не короче
      120 знаков; прогон — 23 из 23 зелёные.
- [x] Корпус `in_positions["parents_children"]` написан в предположении, что у читателя есть дети.
      **Сделано: переписаны все 22 текста.** Было 22 из 22 с прямой отсылкой к детям как к данности
      («С детьми ведёт себя как тренер», «Ребёнку достаётся много тепла»). Стало: каждый текст
      держит обе стороны темы — какое правило получено от родителей и как оно продолжается с теми,
      кто младше или зависит. Ребёнок остался как **один из случаев** в перечислении, а не как
      условие: слово встречается в 3 текстах из 22, все три — в форме «с ребёнком, учеником или
      новичком в команде одинаково».

      Образы прежних текстов сохранены (тренер, «догадываются, но ничего не обсуждают вслух»,
      «правил должно быть меньше, чем разговоров», разбор полётов). Проверка корпуса: 22 из 22 в
      норме 2–4 предложения, зачины все разные, запрещённых формул и канцелярита нет, дублей
      предложений и первых 30 знаков по всему корпусу нет.
- [x] Переиспользование ролей даёт пересечение между персональными статьями одной матрицы.
      **Сделано: пересечение срезано, остаток объяснён читателю.** Причина была в том, что одна
      точка матрицы входит в два раздела и читалась там одним и тем же текстом без рамки роли.

      | Пара разделов | общих 8-грамм до | после | доля меньшего разбора |
      |---|---|---|---|
      | `resources` ~ `money40` | 154 | **69** | 43–66 % → **18–44 %** |
      | `realisation` ~ `purpose` | 214 | **157** | 37–47 % → **21–30 %** |
      | `loops` ~ `rest` | 103 | **83** | 26–35 % → **21–34 %** |

      Что сделано: `money40` переведён на собственный корпус (см. §2.3), а роли, которые читают
      чужую точку, получили свои рамки — `growth_personal` и `growth_social` в `realisation`,
      `loop_root` и `loop_autopilot` в `loops`, `money_entry_mature` в `money40`.

      Остаток убрать правкой кода нельзя: «личное предназначение» — это одна и та же точка карты в
      двух разделах, и предметный текст у неё один. Поэтому в разбор добавлен блок «Те же точки в
      разделе „…"» — он называет общие роли, объясняет, что повтор не ошибка расчёта, и уводит
      ссылкой на второй разбор той же матрицы (`SHARED_ROLES` в `web/lib/sectionReadings.ts`).
- [x] ~~`money.points` ведёт на точку C, которой нет среди ролей~~ — **проверка отменяет пункт**:
      `engine/matrix.py:240` — `m.ground = Triad.of(m.day, m.year)`, то есть «итог земли» (роль
      раздела) считается из точки C. Ссылка верна. Проверил заодно все 11 подобных связей: остальные
      тоже ведут на точки, которые питают формулу раздела.
- [x] ~~Несогласованный суффикс `title`~~ — **не дефект**: правило работает как задумано. Замер:
      `loops` 82 знака и `comfort` 61 — суффикс снимается, `years` 47 — остаётся. Порог 70 знаков
      описан в `site.ts`: длинный заголовок печатается без бренда, чтобы обрезка в выдаче съела
      не смысл.

---

## 6. Вне SEO-скоупа, но лежит в том же staging

Индекс релиза смешивает два независимых набора: 69 файлов SEO-итерации `destiny-matrix` и 10 файлов
не связанной с ней работы в `research-tools` («домены»: `wscore.py`, `server.py`, `frontend/src/*`,
`tests/test_data_layer.py`, `tests/test_ws.py`).

- [x] Разнести по двум коммитам. **Подготовлено; сам коммит за вами** — я в git не пишу.
      Наборы не пересекаются:

      **Коммит 1 — итерация SEO «матрицы судьбы»** (всё, что относится к разбору и контенту):
      `project/destiny-matrix/**`, `tools/seo/**` и `tools/benchmark-testing/out/cycle-13/**` —
      сюда же планы итераций 3 и 4, статьи разделов (`tools/seo/content/` — источник, из которого
      собирается корпус) и три отчёта цикла живого поиска.

      **Коммит 2 — не связанная с итерацией работа по `research-tools`** (10 файлов):
      `wscore.py`, `server.py`, `frontend/src/App.tsx`, `frontend/src/api.ts`,
      `frontend/src/store.ts`, `frontend/src/index.css`,
      `frontend/src/__tests__/app.test.tsx`, `frontend/src/__tests__/store.test.ts`,
      `tests/test_data_layer.py`, `tests/test_ws.py`.

      Команды, если нужны (второй набор — точный список, первый — всё остальное):

      ```bash
      git reset                                   # снять всё из индекса
      git add project/destiny-matrix tools/seo tools/benchmark-testing/out    # коммит 1
      git commit -m "Improve destiny matrix readings and section articles"
      git add wscore.py server.py frontend/src tests/test_data_layer.py tests/test_ws.py
      git commit -m "Update domains view and websocket data layer"
      ```

Отдельно: в контрактном наборе `research-tools` падают 4 теста
(`test_crawl_from_cache`, `test_rebuild_from_cache_gives_the_same_tree`,
`test_connect_backfills_model_from_cache`, `test_crawl_progress_flows_and_total_may_grow`). Они
падают и на `HEAD` — это дрейф частот в `semcore.db`, к релизу отношения не имеет.

- [x] Разобраться отдельной задачей: тесты сверяют оплаченный кэш с текущим состоянием дерева.
      **Решение: в эту итерацию не берём, и вот почему.** Это `research-tools`, а не
      «матрица судьбы»: четыре теста (`test_crawl_from_cache`,
      `test_rebuild_from_cache_gives_the_same_tree`, `test_connect_backfills_model_from_cache`,
      `test_crawl_progress_flows_and_total_may_grow`) падают и на `HEAD` — до итерации 3, так что
      к релизу контента отношения не имеют. Починка требует решения, что считать эталоном при
      дрейфе частот в оплаченном кэше, а трогать `semcore.db` мимо такого решения нельзя. Пункт
      остаётся отдельной задачей, а не хвостом релиза; в чеклисте закрыт как «разобрано».

**Найдено по ходу правок.** Снимок паритета протухает ровно в полночь: `spec/parity-digests.json`
хранит поле `through` с датой, а `engine/tests/test_method_contract.py::test_python_full_range_matches_locked_parity_snapshot`
и `web/lib/matrix.parity.test.ts` требуют, чтобы она равнялась сегодняшней («пересоберите
npm run golden»). Прогон 1 сентября был зелёным, 2 сентября те же два теста красные без единой
правки в коде.

- [x] Сделать проверку диапазонной, а не на точное равенство дат. **Сделано в обоих наборах**:
      `engine/tests/test_method_contract.py:93` и `web/lib/matrix.parity.test.ts:36` требуют, чтобы
      снимок не был из будущего и не был старше 31 дня; конец диапазона для текущего года берётся
      из самого снимка (`through`), а не из сегодняшней даты — иначе на следующий день после сборки
      счётчик случаев разъезжался с зафиксированным.

      Проверено подменой «сегодня» на самом тесте: +0, +1, +30, +31 дней — зелёный, +32 и +60 —
      красный с текстом «снимок покрывает даты до 2026-09-02, сегодня … — пересоберите npm run
      golden», −1 день — красный «снимок датирован будущим». Оба набора зелёные: `pytest
      engine/tests/test_method_contract.py` 8 из 8, `matrix.parity.test.ts` 1 из 1.

---

## 7. Цикл живого поиска дефектов — НЕ ВЫПОЛНЯЛСЯ, надо прогнать

Всё, что описано выше, найдено чтением кода, разбором контента и точечными замерами на стенде.
**Полноценный цикл живого поиска (`tools/benchmark-testing/testing-cycle.md`) не выполнялся.** Он был
запущен и остановлен на первом этапе по лимитам: подняты 12 маршрутов, ни один агент не завершился,
находок ноль. То есть класс дефектов, ради которого этот инструмент существует — **состояние и
последовательности** («купил, ушёл, вернулся, получил отказ, купил снова») — на этом релизе не
проверялся вообще.

Что уже сделано и не надо повторять:

- preflight по §3 пройден: стенд был рассинхронизирован (образ 18:29, исходники 21:02), пересобран и
  перезапущен — `commit 084b2ff+`, `PAYMENT_PROVIDER=mock`, главная гидратируется в чистом контексте,
  все `/_next/static/*` успешны, клиентских ошибок нет;
- **маршруты, которые были заведены** (12): SEO персональных разборов · SEO общих статей · обещания и
  формулировки · мобильный экран · калькулятор и навигация · цели платежа · аккаунт и кабинет ·
  печать PDF · приватность и индексация · дубли и повторные отправки · публичные страницы и 404 ·
  админка. Первые четыре — самые важные для этого релиза.

**Прогон выполнен: цикл 13, 2026-09-02, ступень 1.** Preflight по §3 пройден заново после всех
правок итерации 4: стенд пересобран (`compose/scripts/run.sh`, образ `arcana/web:dev` от 14:10 МСК,
6 357 статических страниц), `payments.active().name == "mock"`, в чистом браузерном контексте главная
гидратируется (поле даты принимает ввод), неуспешных `/_next/static/*` — ноль, аварийного экрана нет,
`/api/health` отвечает `{ok: true, db: true}`. Единственный ответ ≥ 400 на главной — `401` на
`/api/auth/me` у анонимного посетителя, это штатно.

Заодно на пересобранном стенде подтверждены три правки итерации 4: `/encyclopedia/position/day`
отдаёт восьмую главу «Почему визитка расходится с самоощущением», `/encyclopedia/money40/13-8`
читает собственный корпус («в зрелом денежном направлении…»), а `/encyclopedia/arcanum/99` и
`/encyclopedia/chakra/nope` отдают 404 с заголовком «Страница не найдена» и текстом в разметке.

Бриф для агентов написан заново и **сохранён** (в отличие от прошлого раза):
`brief.md`, `known.md`, `routes.json` и `workflow.js` в каталоге цикла, известные дефекты выданы
агентам списком, чтобы отчёт не забился повторами. Источники брифа — те же:
`tools/benchmark-testing/testing-cycle.md` (§2 режим, §3 ступени, §4 маршруты, §9 правила агентам) и
`tools/benchmark-testing/user-testing-agent-rules.md` (шаблон находки, классификация 0/1/2/3, что
дефектом не считается). Модель монетизации, которую надо выдать агентам заранее по правилу §4:
бесплатны расчёт, 2 бесплатных раздела и вся энциклопедия; продаются полный отчёт по конкретной дате
(18 платных разделов) и печать PDF; утечка = персональная интерпретация платного раздела конкретной
даты доступна без оплаты.

Чеклист:

- [x] Прогнать цикл целиком. **Сделано**: 12 маршрутов поиска плюс три независимых опровергателя,
      15 агентов, 0 сбоев. Заявлено **18 находок**, опровергатели подтвердили все 18 — то самое
      «подозрительно много», о котором предупреждает дока, поэтому этап 2 я прошёл сам и каждую
      находку перепроверил своим прогоном.
- [x] Пройти этап 2 «трезвый отсев» самому. **Сделано, и отсев изменил картину:**

      | Исход | Сколько | Что это |
      |---|---|---|
      | дефект, исправлен | **13** | 5 реальных багов, 6 SEO и текстов, 2 вёрстки |
      | ожидаемое поведение | 1 | «Последнее появление» в админке отстаёт на час — так устроен сброс пульсов, это описано в коде |
      | пограничное, вопрос владельцу | 2 | два имени у одной точки карты; дата рождения в заголовке персональной линии |
      | не воспроизвелось у меня | 2 | половина находки про пол (на `/report` дефекта нет, он только на главной) и `«Платежи (1)»` — оказалось расхождением подписей, а не чисел |

      **Одну находку опровергатель подтвердил не там, где она есть.** Про переключатель пола
      заявлено «оплаченный разбор снова под замком»; мой прогон показал, что `/report` открывается
      нормально, а закрывается блок **на главной**. Тест написан на главную — на `/report` он был
      бы зелёным при живом дефекте.
- [x] Собрать три отчёта по §8 доки. **Сделано**: `tools/benchmark-testing/out/cycle-13/` —
      `seo-and-texts.md` (6 дефектов, 2 пограничных), `site-bugs.md` (5 дефектов, 3 отсеянных),
      `layout.md` (2 дефекта, 1 отсеянное). В каждом обязательные столбцы «когда возникает» и
      «почему не находили раньше» заполнены парой «есть / нет» и названием слепой проверки.
- [x] Учесть, что дефекты из §1 и §5 цикл найдёт заново. **Сделано**: перед прогоном известные
      вещи выданы агентам файлом `known.md` (шесть строк: остаточный 404 динамического маршрута,
      один лист PDF, пересечение разборов, слаги `money40`/`resources`, `noindex` персональных
      страниц, `/report` без параметра). Ни один агент их не заявил — повторов в отчётах нет.
- [x] Отдельно решить по ступени 2. **Решение: в этой итерации не запускаем.** Ступень 2
      переключает провайдера оплаты на весь стенд и идёт последовательно по ~40 с на платёж; она
      про точность денежных ветвей («отказ по карте», «отмена», «возврат», гонка двух вкладок),
      которых мок не создаёт вовсе. Запускать её имеет смысл **после** этих правок — они трогают
      цель платежа и право доступа, то есть ровно то, что ступень 2 проверяет. Нужна явная
      авторизация владельца: ключи тестового терминала лежат вне репозитория, а прогон уходит в
      чужую систему. Дефекты цикла 13 про деньги (кнопка шапки, переключатель пола) на моке
      воспроизвелись и закрыты браузерными тестами.

### Что цикл 13 нашёл и что с этим сделано

| Категория | Дефектов | Главное |
|---|---|---|
| реальные баги сайта | 5 | кнопка «Купить» в шапке платила за другую дату; переключатель пола закрывал оплаченный разбор; «Мой разбор» в кабинете вёл на неоплаченную дату; 13 ссылок в купленном PDF вели на внутренний адрес контейнера |
| SEO и тексты | 6 | на 31 382 адресах `noindex`, `canonical` и `<title>` приезжали после `</head>`; «для даты undefined undefined 0» в описании 5 544 страниц; оферта обещала бесплатным раздел, которого нет; формула визитки расходилась с движком |
| вёрстка | 2 | четырёхрольные разделы на 320 px выезжали текстом за карточку (регрессия правки §1.1); 404 набиралась запасными шрифтами |

Три вывода, которые стоит унести в правила:

1. **Место тега — тоже инвариант.** Проверка «в разметке есть `noindex`» проходила, пока тег
   лежал в `<body>`. Формулировка должна называть место: «в подстроке до `</head>`».
2. **Тест обязан мерить ту поверхность, где живёт дефект.** Ссылки в PDF проверялись по экрану
   отчёта; в PDF они другие, потому что печать открывается по внутреннему адресу.
3. **Одна находка — две поверхности.** Дефект пола есть на главной и отсутствует на `/report`:
   если бы тест написали по тексту заявки, он был бы зелёным при живом дефекте.

## 8. Порядок работы

1. **Сначала §1** — это то, что видит покупатель в отчёте и в PDF. Пункты 1.1, 1.2 и 1.3 чинятся в
   двух файлах (`roleContent.ts`, `sectionReadingShared.ts`) и закрывают разом 9–14 разделов.
2. **Потом §4.1 и §4.2** — без починенных сторожей следующая итерация повторит то же самое.
3. **Потом §2 и §3** — содержательная работа, её объём стоит оценить после §1: часть претензий к
   текстам снимется сама, когда до страниц доедут выброшенные предложения (§1.3).
4. **Потом §7** — цикл живого поиска. Раньше §1 его гонять бессмысленно: маршрут «обещания и
   формулировки» и маршрут «мобильный экран» вернут те же дефекты, что уже перечислены здесь.
5. **§5 и §6** — по остаточному принципу.

Правило, которое стоит унести в следующую итерацию: **автотест, проверяющий, что две строки не
равны, ничего не проверяет, если строки различаются служебным префиксом.** Ровно так прошли
проверку четыре пары ролей (§2.1) и 66 модификаторов чакр.
