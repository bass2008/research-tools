# Требования к статье энциклопедии (SEO + смысл и качество)

Спека для написания страниц `arcana-sense.ru`: арканы, позиции, сочетания, концепт-хабы
(программы, карма, хвост), матричные тройки. Предметный источник —
`project/destiny-matrix/docs/methodology.md`; поисковая фраза не может переопределять формулу.

Главный принцип: **поисковик оценивает не «чем написано», а пользу и уникальность по смыслу.**
LLM — нормальный инструмент. Штрафа «за ИИ» нет. Штраф есть за страницу, которая ничего нового
не добавляет. Поэтому качество здесь — не про красоту слога, а про то, есть ли у страницы
**свой смысл** и **закрывает ли она вопрос человека**.

---

## Часть A. Смысл и качество (главное)

### A1. Уникальность — по смыслу, а не по словам
Разные слова при одинаковом смысле — для Яндекса тот же дубль (фильтр «малополезный контент»
сравнивает смысл, а не буквы). Значит, недостаточно «переписать другими словами». Страница
обязана нести то, чего нет на соседних страницах.

**Тест перед написанием:** «Что эта страница говорит такого, чего НЕТ на страницах её частей?»
Если ответа нет — писать нечего, страницу не делаем (или закрываем в `noindex`).

### A2. Правило целого: пиши про взаимодействие, а не про сумму частей
Самая частая ошибка — «склейка»: страница сочетания/тройки = пересказ куска про аркан A + куска
про аркан B. Это дубль в новых словах: смысл уже лежит на `/arcanum/A` и `/arcanum/B`.

- **Страница аркана** — про сам аркан во всех позициях (её уникальность: все позиции вместе).
- **Страница сочетания/тройки** — про то, что происходит, **когда они стоят вместе**: усиление,
  конфликт, во что складывается, что делать. Ни одного абзаца, который можно дословно перенести
  на страницу одиночного аркана.
- **Кармический хвост** — только ordered-последовательность M–N–D из реестра метода. Первое,
  второе и третье числа имеют разные роли. Сортировка, разворот и редирект перестановок запрещены.
- **Программа** — только сочетание с подтверждённой позицией. Три числа без позиции не становятся
  программой автоматически, даже если такое название встречается у конкурента.
- **Концепт-хаб** (программы, кармический хвост) — про сам механизм: как считается, как читается,
  типовые сценарии, частые ошибки. Не список арканов.
- **Хаб vs раздел разбора (антиканнибализация, E3):** когда хаб и позиция целятся в одну фразу
  (`/programmy` ↔ позиция `loops`; `/energii` ↔ чакры), делим интент: **хаб** — «что это и как
  считается» (механизм), **позиция/раздел** — «как это читается в вашей карте». Позицию не
  оптимизируем под хаб-запрос, хаб не пересказывает разбор позиции.

### A3. Соответствие интенту (зачем человек пришёл)
Одна страница — один вопрос человека. Определи его точно и ответь на **него**, а не на смежный.

- «6 в матрице судьбы» — человек хочет *значение* числа 6, а не калькулятор. Отвечаем значением.
- «рассчитать матрицу онлайн» — хочет инструмент, а не статью. Такой запрос — на главную, не сюда.
- Ответ на главный вопрос — **в первом экране**, без раскачки и без «немного истории таро».

### A4. Польза = вопрос закрыт на странице
Человек не должен уходить дочитывать к конкуренту. Признаки закрытого вопроса:
- есть прямой ответ, а не «всё индивидуально, запишитесь на консультацию»;
- есть конкретика: примеры, «сильная/слабая сторона», «что с этим делать»;
- есть следующий логичный шаг (соседний аркан, своя карта) — через ссылки, а не тупик.

### A5. Тон и экспертность (тематика — эзотерика, YMYL-смежная)
- Уверенно и конкретно, но без обещаний событий и без медицины/гарантий (см. дисклеймер сайта).
- Не лить воду ради объёма: длина берётся из полноты ответа, а не наоборот (см. C).
- Живой связный текст, а не список ключей. Переспам ключами → фильтр переоптимизации
  (Баден-Баден). Ключ входит естественно.

### A6. Антипаттерны смысла (красные флаги = переписать)
- Абзац дословно применим к другой странице → это чужой смысл, убрать.
- Статья = склейка кусков про части (см. A2).
- «Вода»: общие слова про судьбу/энергии без привязки к конкретной сущности страницы.
- Ответ на вопрос человека появляется ниже середины страницы (или его нет).
- Страница сделана под запрос, которого никто не ищет (см. C1).

---

## Часть B. SEO на странице (on-page)

### B1. Title
- Точная формулировка запроса в естественном виде, ключ ближе к началу. Для арканов —
  вариант **«N в матрице судьбы»** (топ-формулировка без слова «аркан»), а не только «N аркан».
- До ~60–70 знаков — это про **сам `seo.title`, без бренда**: суффикс « — Arcana Sense» дописывает и,
  если сумма вылезает за 70, снимает шаблон. Запас под бренд не закладывать, заголовки не сокращать впустую.
- Уникален на весь сайт.

### B2. H1
- Один на странице, отражает суть и содержит ключ. H1 «Влюблённые» без числа и «матрицы судьбы»
  — слабо; нужно, чтобы H1 бился с запросом человека.
- H1 ≠ title дословно, но об одном.

### B3. Description
- 1–2 предложения, живые, с ключом и с «крючком» (что человек получит). Влияет на кликабельность,
  не на ранг напрямую — но клик влияет на ранг.
- Уникален на каждую страницу (не шаблон с подстановкой числа).

### B4. URL / slug
- Короткий, латиница/цифры, человекочитаемый, стабильный (менять URL после индексации — терять
  позиции). Схему не плодить: тип сущности + идентификатор.

### B5. Структура
- Ответ на главный вопрос — в первом экране.
- Разбивка на H2/H3 по подвопросам (значение, в позициях, плюсы/минусы, сочетания, «что делать»,
  вопрос-ответ). Заголовки — осмысленные, с подтемами (это тоже сбор хвоста).
- Абзацы короткие, списки где уместно, таблицы для «позиция → значение».

### B6. Ключи и хвост
- Один главный интент на страницу + его синонимы/словоформы (LSI) естественно по тексту.
- Подзаголовки закрывают близкие хвосты («что значит», «в центре», «в отношениях», «плюсы и минусы»).
- Никакого повторения ключа ради плотности.

### B7. Внутренняя перелинковка (обязательна, в обе стороны)
- Тупиков быть не должно: с каждой страницы — ссылки на соседние арканы, позиции, сочетания и на
  свой расчёт. Обратные ссылки тоже (аркан ↔ позиция ↔ сочетание).
- Из индексируемой статьи — заметный вход в расчёт (конвертация инфо-трафика в лид). Предпочтительно **встроенный
  мини-калькулятор** (`CalcPromo`), а не только ссылка: без входа в расчёт инфо-трафик уходит без
  действия. Он обязателен на всех инфо-шаблонах (арканы, чакры, сочетания, позиции), не только на арканах.
- **`related` — односторонний от автора** (ставит по смыслу, A → B). Обратную ссылку (B → A) строит
  шаблон автоматически, инвертируя граф `related`; авторам не проставлять взаимность вручную и не
  править сданные статьи при добавлении новых. Ключ статьи = URL, уникален в категории.

### B8. Разметка Schema.org
- `Article` (headline, description, inLanguage, image, mainEntityOfPage).
- `BreadcrumbList` под хлебные крошки.
- `FAQPage` — если есть блок вопрос-ответ (даёт расширенный сниппет).

> **Решение по FAQ (принято): арканы и позиции — БЕЗ FAQ.**
> Причины: под-вопросы («N хорошо/плохо», «редкое ли число», «что такое центр») почти не имеют
> спроса → трафика не несут; ответы на них across 22 арканов / 37 позиций получаются шаблонными
> (одинаковые формулировки) — это «AI-след» и внутрикластерный near-dup в размеченном блоке, риск
> фильтра малополезного; сам FAQ rich-snippet сейчас почти не выдаётся (Google — в основном
> gov/health, Яндекс — без гарантии). Уникальность страницы несут `meaning` + готовая таблица
> позиций/арканов, FAQ там ничего не добавляет, только риск.
> **FAQ оставляем только там, где Q&A содержателен и разнообразен:** концепт-хабы и «матрица на год»
> (статьи с нуля, вопросы специфичны теме и со спросом — «как рассчитать год», «что такое хвост»).
>
> **То же по секциям: у арканов и позиций НЕ делаем `sections[]` по сферам жизни** (в отношениях,
> деньги, центр) — эти грани уже покрыты таблицей (`in_positions` у арканов, блок «22 аркана в
> позиции» у позиций), отдельная секция = внутристраничный near-dup. Обогащаем только `meaning`
> (суть) + `seo`. `sections[]` нужны там, где таблицы нет: концепт-хабы, «на год», тройки-хвосты.

### B9. Медиа
- У картинок осмысленный `alt` с сутью (не «изображение»). Латиницы на артах карт быть не должно.
- Изображения не должны ронять LCP первого экрана.

### B10. Индексация
- `canonical` на себя.
- Нет спроса, но сущность нужна продукту → `noindex, follow`: страница живёт и передаёт ссылочный
  вес, но в поиск не лезет. Нет сущности выбранного метода и полезного ответа → настоящий 404/410,
  без редиректа на хаб и без коммерческого CTA.

---

## Часть C. Когда вообще писать и сколько

### C1. Спрос-гейт (до написания)
Полноценную статью пишем только под запрос, у которого есть спрос (частота в ядре
`logs/needs-lab/матрица-судьбы-dd5dcc98/params.json`). Нет спроса → шаблон + `noindex`, время не
тратим. Особый случай — **матричные тройки**: сперва проверить, ищут ли их вообще и в какой форме
(арканы / дата / кармический хвост), и лишь совпавшие с реальным спросом достойны статьи.

### C2. Объём — по полноте ответа, не по норме
Ориентиры (нижняя граница «не тонко», не самоцель):
- Аркан / крупная позиция / концепт-хаб: полный разбор, обычно от ~3–5 тыс. знаков.
- Сочетание / тройка со спросом: столько, чтобы раскрыть именно взаимодействие, без воды.
- Меньше — только если вопрос узкий и честно закрывается коротко.

---

## Часть D. Формат сдачи

Статья — это **данные для шаблона, а не готовая страница.** Сайт рендерит из структурированного
контента (`content/*.json`) + TSX-шаблон; шаблон сам строит `<title>`/H1, canonical, хлебные крошки,
перелинковку, карточки арканов, CalcPromo и JSON-LD (`Article`/`BreadcrumbList`/`FAQPage`). Поэтому
статью сдают **как JSON-объект, совпадающий со схемой своей категории** (та же семья, что
`content/arcana.json`, `content/combinations.json`).

### D1. Что пишет автор, что делает шаблон
Автор заполняет **только текстовые поля**:
- `seo`: `{ title, description, queries[] }`
- `title` / `h1`, `short` (лид первого экрана)
- проза — **массивом абзацев** (`meaning` / `paragraphs`), а для богатых страниц
  `sections[]`: `{ h2, paragraphs[] }` и `faq[]`: `{ q, a }`
- связи — **списком идентификаторов** (`arcana: [9, 18]`, `related: ["9-6-9", …]`)

Внутри `paragraphs`/`short` — **чистый текст: без HTML, без markdown-ссылок `[текст](/)`, без
заголовков.** Заголовки, ссылки, крошки, схему и калькулятор вставляет шаблон.

### D2. Схема — по категории
Поля одинаковы у всех новых категорий, различается только ключ и его смысл. Обязательны
`key`/`n`, `title`, `seo.title`, `seo.description`, `short` (от 60 знаков) и хотя бы одна секция с
абзацем: без любого из них загрузчик **отбрасывает статью целиком**, страница не появляется, а
сборка печатает `[content] N статей отброшено валидацией`. `faq`, `related`, `arcana` — необязательны.

| Категория | Файл | Ключ | Особое |
|---|---|---|---|
| Кармические хвосты | `content/karmic-tails.json` | `key` — точный порядок M–N–D: `"18-9-9"` | `arcana` обязана дословно совпадать с key; перестановки не редиректят друг на друга; `publication.index` задаёт demand gate |
| Матрица судьбы на год | `content/year-arcana.json` | `n` — номер аркана `"7"` или год-штамп `"2026"` | у аркана 1…22 шаблон рисует карту и ссылку на `/encyclopedia/arcanum/N`; у года — только заголовок |
| Шапки категорий | `content/category-hubs.json` | `key` — `"karmic-tail"` или `"na-god"` | заменяет встроенный текст хаба; без статьи хаб живёт со своим текстом |
| Концепт-хабы | `content/hubs.json` | `key` — слаг адреса: `"programmy"` | ключ обязан совпадать с существующим роутом (`ROOT_HUBS`), иначе страницы не будет; `related` — только номера арканов |

Пример для кармического хвоста:
```json
{
  "key": "9-9-18",
  "arcana": [9, 9, 18],
  "entity_type": "karmic_tail",
  "publication": { "index": true, "follow": true, "primary_query": "кармический хвост 9 9 18" },
  "title": "…",
  "seo": { "title": "…", "description": "…", "queries": ["кармический хвост 18 9 9", "…"] },
  "short": "…",
  "sections": [ { "h2": "…", "paragraphs": ["…", "…"] } ],
  "faq": [ { "q": "…", "a": "…" } ],
  "related": ["18-9-9", "arcanum/9", "arcanum/18"]
}
```

### D3. Куда
Один объект на страницу, в источник данных категории (напр. массив `items` в
`content/karmic-tails.json`). Markdown в `tools/seo/out/*.md` — это **только человекочитаемый
предпросмотр для ревью** (как `result-article-1-1-2.md`), в сайт он не идёт.

---

## Часть E. Пожелания разработки

Этот раздел писала **разработка**, а не SEO: он про то, обо что спотыкается публикация, когда
статьи пишет не тот, кто писал шаблон. Всё перечисленное **выполнено в общем скоупе** доработок
сайта под SEO — здесь оно зафиксировано как правило, а не как задача.

### E1. Схема есть у каждой категории, не только у хвостов — выполнено
Раньше D2 описывал одну категорию из четырёх, а схема остальных жила в коде. Теперь в D2 стоят все
четыре с ключами и обязательными полями. Проверять себя по строке сборки: если статья не появилась,
сборка уже сказала, сколько статей отброшено и каких полей им не хватило.

### E2. Ключ статьи не переименовывают — выполнено
Ключ и есть URL. После того как страница попала в индекс, смена ключа означает потерю страницы:
старый адрес требует записи в `tools/seo/audit/public-url-map.json`. Redirect допустим только на
однозначного successor той же сущности. Для семантически другой перестановки, хаба или главной
редирект запрещён; ошибочная сущность получает noindex на переходный период либо 404/410.

### E3. Хаб и позиция не спорят за один запрос — выполнено
Концепт-хаб (`/programmy`) и раздел разбора (`/encyclopedia/position/loops`) легко оказываются на
одном запросе: два своих адреса под одну фразу делят между собой её же выдачу. Развести может только
текст, разметка не поможет. Правило: **хаб отвечает «что это такое и как считается» в общем виде**,
раздел — **«как это читается в вашей карте и что с этим делать»**. Если абзац хаба переносится в
раздел без изменений, один из двух текстов лишний.

### E4. Новые точки входа считаются в Метрике
Каждая форма расчёта на инфо-страницах отправляет цель `calc` с признаком места: `arcanum`,
`position`, `chakra`, `combination`, `karmic-tail`, `karmic-tail-hub`, `na-god`, `na-god-hub`,
`hub-<ключ>` (у главной и отчёта свои места: `landing`, `allbox`, `report*`). Событие несёт landing,
entity/place и анонимные visitor/session/tab, но не дату рождения, почту, JWT или сырой платёжный
идентификатор. Цели в интерфейсе Метрики проверяются отдельным pre-release пунктом: код не может
создать их без доступа владельца к внешнему счётчику.

### E5. Год-штамп обновляют в декабре — выполнено
Страница «матрица судьбы на 2026» — единственная в корпусе, которая устаревает по календарю. Ключ
статьи равен году, поэтому новая страница пишется как новая статья (`"2027"`), а прошлогодняя
остаётся: она уже проиндексирована, и её адрес не переиспользуют. Первую неделю января такой запрос
приносит трафика больше, чем весь остальной год, поэтому текст готовят заранее.

---

## Чеклист «готово к публикации»
- [ ] Есть спрос на запрос страницы (C1).
- [ ] Определён один интент, ответ на него — в первом экране (A3).
- [ ] Сущность и точный порядок существуют в контракте; программа имеет позицию.
- [ ] Ни одного абзаца, переносимого дословно на другую страницу (A2, A6).
- [ ] Страница говорит то, чего нет на страницах её частей (A1).
- [ ] Title содержит точную формулировку запроса, ≤ ~70 знаков, уникален (B1).
- [ ] H1 бьётся с запросом (B2); description живой и уникальный (B3).
- [ ] Структура H2/H3 по подвопросам, таблицы/списки где нужно (B5).
- [ ] Ключ входит естественно, без переспама (A5, B6).
- [ ] Перелинковка в обе стороны, тупиков нет, есть CTA на расчёт (B7).
- [ ] Schema: Article + BreadcrumbList (+ FAQPage при Q&A) (B8).
- [ ] `alt` у картинок, canonical на себя (B9, B10).
- [ ] Нет медицины, гарантий и обещаний событий (A5).
- [ ] Сдано как JSON-объект по схеме категории: проза в полях, без HTML/ссылок/заголовков в тексте (D1–D3).
- [ ] Обязательные поля на месте, сборка не отбросила статью (D2, E1); ключ выбран окончательно (E2).
