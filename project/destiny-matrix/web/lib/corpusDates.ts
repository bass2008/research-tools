// Дата публикации корпуса. Отдельным модулем без единого импорта: её читает клиентская форма
// расчёта (`lib/today`), а Next вшивает в браузерный чанк всё, что лежит в импортированном
// модуле. Поэтому рядом с ней не должно быть ничего подвижного — дата правки корпуса живёт
// отдельно, в `lib/corpusModified`, и по той же причине.
export const CONTENT_PUBLISHED = "2026-04-01";
