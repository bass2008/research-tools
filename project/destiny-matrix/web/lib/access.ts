import { publicSettings } from "./settings/public";

/**
 * Витрина без оплаты: разбор любой даты открыт всем, включая незарегистрированных, а касса
 * закрыта. Нужна рынку, где эквайринга ещё нет.
 *
 * Значение вшивается на сборке: страницы матриц и энциклопедии печатаются заранее, и решать
 * это на запрос им негде. Парную проверку делает api (`ALL_FREE_WITHOUT_PAYMENT`) — фронт
 * открывает текст, а права на печать PDF и хранение всё равно считает он.
 */
export const ALL_FREE: boolean = publicSettings.get("allFree");
