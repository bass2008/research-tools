# web — фронт «Матрица судьбы»

Next.js 16 (App Router, RSC + SSG, сборка Turbopack), TypeScript. Контракт — `docs/api-contract.md`, он главнее кода.

```bash
npm ci
npm run dev        # http://127.0.0.1:3000
npm run build      # около 6 тысяч маршрутов, включая 5544 предвычисленные матрицы
npm start          # прод-сервер
npm test           # vitest: порт движка против эталона Python
npm run typecheck
```

## Что где

| Путь | Что |
|---|---|
| `lib/matrix.ts` | порт `engine/matrix.py` в TypeScript — расчёт идёт в браузере, дата не уходит на сервер |
| `lib/sections.ts` | порт `engine/sections.py`: 20 разделов, 2 бесплатных |
| `lib/__fixtures__/golden.json` | 42 полных golden-вектора; полный parity-снимок содержит 92 526 расчётов |
| `lib/arcana.ts` | корпус 22 арканов (нумерация матрицы, Шут — 22-й) |
| `lib/encyclopedia.ts` | состав энциклопедии: 22 аркана + 37 позиций + 231 сочетание + 7 чакр + корень = 298 |
| `lib/tariffs.ts`, `lib/tariffs.server.ts` | тип и формат цены; server-only загрузчик прайса из базы |
| `lib/analytics.ts` | цели Метрики; фильтр не пускает в параметры дату рождения |
| `lib/settings/` | startup-менеджеры публичных и server-only настроек фронта |
| `components/matrix/` | карта и разбор: октаграмма, таблица чакр, форма расчёта, разделы |
| `components/pay/` | оплата: форма, автомат экрана, тарифы и цена |
| `components/enc/`, `account/`, `admin/`, `ui/` | энциклопедия, кабинет, админка, каркас страниц |

## Переменные окружения

Смотри `.env.example`. `NEXT_PUBLIC_METRIKA_ID` пустой — счётчик не подключается, цели всё равно
пишутся в `dataLayer`. `API_ORIGIN` включает прокси `/api/*` на `apps/api` в разработке.

Все app-owned переменные загружаются только в `lib/settings/public.ts` и
`lib/settings/server.ts`, после чего читаются из `SettingManager` в памяти. BFF
`GET /api/admin/settings` сначала подтверждает admin-сессию в API, затем объединяет группы
`Frontend` и `Backend`. Значения с признаками пароля, токена или ключа автоматически обрезаются;
полный секрет в ответ и React-компонент не попадает.

## Правила, которые легко нарушить

- **Дата рождения не уходит с клиента.** Она живёт в `sessionStorage`, не попадает в URL оплаты и
  в цели Метрики. На сервер уходит только по кнопке «сохранить матрицу» у авторизованного.
- **Контракт формул — `spec/method.json`.** Python и TypeScript исполняют один граф имён и
  обязаны пройти те же golden-векторы и exhaustive parity всех дат с 1900 года.
- **Медицинских формулировок и обещаний гарантий быть не должно.** Единственный список правил
  собирается из `content/data/text-policy.json`; Python-валидатор, загрузчик и build-gate
  проверяются на общем наборе контрольных фраз.
- **Цена нигде не зашита в разметку.** Прайс живёт в базе; страницы с ценой (`/`, `/pay/*`,
  `/oferta`) печатаются на запрос и читают его через `getTariffs()`. Статика печатает «уточняется»
  и подставляет живую цену после гидрации (`TariffsProvider` → BFF `/api/tariffs`): показать число,
  по которому не получится купить, хуже, чем не показать ничего.
- **Цена рекламируемого тарифа обязана быть в первом экране** и в машиночитаемом виде (JSON-LD).
- **Реквизиты — заглушки вида `⟨ИНН⟩`** в `lib/site.ts`. Перед запуском заменить и проверить,
  что заглушек на страницах не осталось.

## Приёмка сборки

```bash
npm run build && npm run check
```

`scripts/check-build.cjs` берёт статику из `.next/server/app`, а страницы по запросу
(`/`, `/oferta`, `/pay`, `/pay/single`) — с сервера, который поднимает сам на порту 3131
(`CHECK_PORT`). Валит сборку, если: нет `title`/`description`/`canonical`/`og:image`/favicon;
есть медицинская лексика или обещание гарантии; есть `href="#"` или битая внутренняя ссылка;
цена рекламируемого тарифа не совпала с прайсом `/api/tariffs`, не попала в первый экран или
в JSON-LD; в юридических страницах пропали
заглушки реквизитов; в ссылке оплаты появилась дата.

Для полной приёмки API должен работать на `127.0.0.1:8010`: без него страница оплаты честно
скрывает недоступную цену и не создаёт счёт.

Сгенерированный контент из `content/*.json` обязателен целиком. Неполный файл или поле с
медицинской лексикой останавливает сборку в `lib/content.ts`; встроенного запасного корпуса нет.
Поэтому отчёт и энциклопедия всегда читают одну версию текста.
