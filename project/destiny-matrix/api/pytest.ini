[pytest]
testpaths = tests
addopts = -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
markers =
    bug: регрессия на дефект, найденный живым прогоном по сайту
    open_bug: дефект найден, но ещё не исправлен — тест красный намеренно (-m open_bug)
