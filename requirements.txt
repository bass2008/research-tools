httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.29      # [standard] тянет websockets для WS-канала
# LLM-транспорт (пакет task-worker-mcp ставится отдельно: pip install -e task-worker-mcp)
mcp>=1.2.0
# тесты
pytest>=8
pytest-asyncio>=0.23
pytest-playwright>=0.5
